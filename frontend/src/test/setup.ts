import { configure } from '@testing-library/dom'

/*
 * Ожидание `findBy*`/`waitFor` — 3 с вместо 1 с по умолчанию.
 * ⚠️ Не «чтобы проходило»: утверждения те же, меняется только предел терпения.
 * Тяжёлые экраны (пародонтограмма, фиша) в полном прогоне на занятой машине
 * рисуются по 1–1,5 с — первый тест файла платит ещё и за разбор модулей, — и
 * секундный предел красил их через раз (25.09, B4.3), хотя поодиночке те же
 * проверки проходят за 400 мс. Красное, которое не воспроизводится, учит не
 * верить прогону — это хуже, чем две лишние секунды на настоящем отказе.
 */
configure({ asyncUtilTimeout: 3000 })
