"""Адрес репозитория, откуда программа берёт обновления — ОДНО место.

Он был скопирован в четырёх файлах (программа и три скрипта выпуска). Пока
адрес не менялся, это ничего не стоило; при переезде расхождение означало бы,
что часть инструментов смотрит в старый репозиторий, а программа — в новый,
и заметно это стало бы только на живом обновлении.

⚠️ Репозиторий с релизами обязан быть ПУБЛИЧНЫМ. Клиника на канале stable
спрашивает `/releases/latest` анонимно; у приватного репозитория анонимный
запрос получает 404, то есть автообновление перестаёт работать у всех сразу,
и вернуть его можно только выдав каждой клинике токен — ровно тот секрет,
которого канал beta и позволяет не держать.
"""
from __future__ import annotations

REPO = "olegbacalu-maker/dental-booking-bot"
