# Дизайн-система

⏳ **Строится с 25.09.2026 (B5).** Шаг 1 сделан: форма интерфейса выражена
СТУПЕНЯМИ в `:root` panel.css (раздел ниже), видимых изменений нет. Шаг 2
сделан: **Fluent — четвёртый стиль** в `theme.STYLES`, без библиотеки. Дальше
по порядку: выбор меню (фирменное по умолчанию / нейтральное) → выбор шрифта →
экран «Aspect» → единые кнопки и поля → журнал и одонтограмма на ступенях.
Границы из следующих разделов остаются в силе.

## Fluent — четвёртый стиль (B5, шаг 2 — 25.09.2026)

Стиль — запись в `theme.STYLES`, как и три прежних; библиотека
`@fluentui/react-components` НЕ ставится: понравившийся Олегу вид — это
значения Fluent 2, а не её компоненты (решение 25.09). Что в записи:

| | modern | fluent |
|---|---|---|
| основа окна / слой / карточка | `#F6FBF8` / — / `#FFF` | `#F3F3F3` / `#F9F9F9` / `#FFF` |
| тексты 2/3 | сине-серые `#7E8B9C` `#A0AAB8` | нейтральные `#616161` `#707070` |
| радиусы card/block/ctl/chip/s/xs/tab | 18/14/12/10/8/6/пилюля | 8/8/4/4/4/4/4 |
| тени покой / наведение / поднятая | двухслойные | нет / shadow4 / shadow16 |
| высоты ctl/-s/-m/nav/top | 44/36/40/44/72 | 32/28/32/36/56 (под пальцем — прежние) |
| h1 / h2 | 32·700, планка, `--teal-d` | 28·600, без планки, `--text` / 20 |
| поле в покое / в фокусе | кромка `--line`, кольцо 3px | нижняя кромка `#8A8A8A`; в фокусе низ 2px `--teal-d` |
| подскоки при наведении | −1/−2/−3px | 0 |
| меню | пункт 44px, 500, подпись капителью | 36px, 400, полоска 3px у активного, подпись 12px |

⭐ У всех четырёх стилей ОДИН набор ключей — `test_theme` держит: предпросмотр
в «Aspect» кладёт переменные инлайном на `<html>`, и ключ без пары у соседа
пережил бы переключение назад. `modern` сверяется с `:root` по всем ключам и с
третьей записью в тесте.

⚠️ Вшитый оттенок наведения `#0B7E6D` (установка без выбора — байт в байт)
берётся только там, где он читаем на фоне стиля: на `#F3F3F3` он даёт 4.49, и
Fluent получает расчётный. Стиль новый, беречь в нём нечего.

⚠️ Единственная видимая правка старых стилей на этом шаге: строка фильтров
«Pacienți» (`.pl-search`, `.pl-bar select`, `.pl-btn`) и кнопка списка в фише
(`.lbtn`) были 42px при общих 44 — выровнены на `--h-ctl`. Под Fluent 42 рядом
с 32 читалось бы как ошибка.

Стиль по умолчанию остался `modern` — решение за Олегом (открыто).

## Кнопки и поля — одно определение на семейство (B5, шаг 6 — 25.09.2026)

До этого шага фирменная кнопка была описана в 16 местах, контурная в 12, поле
ввода в 12 — и каждая копия чуть своя (11 размеров шрифта, 24 варианта
отступов, у половины кнопок не было `font-family:inherit`, и они рисовались
системным шрифтом посреди страницы в Inter). Теперь в `panel.css` › «контролы»
четыре рецепта, каждый ОДИН раз, со всеми состояниями (наведение, нажатие,
фокус с клавиатуры — кольцо `--ring`, запрет):

| рецепт | кто в списке | что унифицировано |
|---|---|---|
| фирменная | `.savebtn`, `form.add button`, `.fform button`, `.dlg-form button`, `.lbtn`, `.top .newbtn`, `.nav a.primary`, `.nav form.searchf button`, `.dp-fw-btn`, `.dp-ok-btn` | teal / on-teal, `--h-ctl`, `--r-ctl`, 0 18px, 14·600, hover teal-d + `--sh-t` + `--lift` |
| контурная | `.nav a`, `.pl-btn`, `.filepick label`, `.hero-acts *`, `.dvacts *`, `.dp-pc-btn`, `.actmore` (+ размер `-m`: 40px, 13.5) | panel / line / text2, `--h-ctl`, 0 16px, 14·500, hover teal + `--sh-hover` + `--lift` |
| тихая | `.dlg-head button`, лупа поиска, `.alert button`, `.doc form button`, `button.pt` | без фона и рамки, кольцо фокуса |
| поле | `.nav input.dpick`, поиск раздела, `.dlg-form`/`form.add`/`.fform` input·select·textarea, `.pl-bar select`, `.pl-pag select`, `table.set` (компакт) | `--h-ctl`, 0 14px, кромка + низ `--field-line-b`, `--r-ctl`, 14, фокус `--focus-line(-b)` + `--ring`, placeholder text3 |

Глобально: `button,input,select,textarea{font-family:inherit}`.

⚠️ Границу у фирменной кнопки рецепт СНИМАЕТ (`border:none`): первая версия
её не задавала, и `<button>` нарисовал свою серую рамку по умолчанию — на
кадрах это выглядело как «полупрозрачная кнопка с кромкой». Одна
`.nav a.primary` возвращает себе 1px — ровняется с соседями по строке.
⚠️ Кнопки поверхностей зуба (`.sfbtns .sfbtn`) лежат внутри `.dlg-form` и
попадают под рецепт: отступ 0 18px не влезал в сетку из пяти клеток, пятая
вылезала за карточку — у них свой `padding:0 4px`. ⚠️ Внутри `.dlg-form`
живут и кнопки не фирменные
(«Nu» переноса `.mv-no`, фишки ролей моста `.br-chip`): специфичность
`.dlg-form button:hover` (0,2,1) выше их (0,2,0), поэтому у них свои `:hover`.
⛔ Вне рецептов остались кнопки цвета СМЫСЛА (`.act`, `.bstat` — исход визита;
`.dp-upd-btn` — обновление), переключатели-сегменты (`.tabs`, `.tabbtn`,
`.halfpick`, `.viewsw`), поля пародонтограммы (`.pcell`, `.pmf`, `.pfoot`,
`.pinline` — клиническая часть, отдельный шаг) и мелкие чипы.

Что изменилось видимо на старых стилях (стенд): кнопки без `font-family` —
теперь Inter; `.savebtn` 0 26px → 0 18px; `.dlg-form button` — высота
`--h-ctl` и `--r-ctl` вместо 10px/чипа; `.pl-btn`/`.filepick` 13.5 → 14;
`.dvacts` 38 → 40; `.nav input.dpick` и `.pl-bar select` — цвет текста
`--text` вместо `--text2`; запрещённая кнопка диалога — полупрозрачная вместо
серой. Всё это — сведение к одному значению, не новый облик.

## Меню и шрифт — два выбора рядом со стилем (B5, шаги 3–4 — 25.09.2026)

Ответы Олега 25.09: «меню фирменный зелёный цвет по умолчанию», «включить в
настройках переход между шрифтами», «стили оставить». Оба выбора устроены как
стиль — НАБОРОМ переменных, а не правилами:

- **Меню** — `theme.MENUS`: `brand` (умолчание; заливка `--teal-d`, текст
  `--on-teal-d` — то, что клиника видит с 08-17) и `neutral` (на основе окна,
  текстом интерфейса; полоска активного пункта и всё цветное — фирменные).
  Набор — группа `--side-*` плюс кромка и угол слоя содержимого
  (`--layer-line`, `--layer-r`): нейтральное меню отделяется от слоя линией и
  скруглённым углом, иначе две одинаковые поверхности сливаются. `brand`
  обязан повторять `:root` (test_theme), у обоих один набор ключей.
- **Шрифт** — `theme.FONTS`: `inter` (вшит в программу, одинаков на любой
  машине) и `system` (Segoe UI — тот, которым написан Windows; на 10 и 11 он
  немного разный, об этом говорит подпись варианта). Значение уходит в `--font`
  журнала, а страницам со своей вёрсткой (вход, PIN, восстановление) — тем же
  набором семейств через заполнитель `__FAMILY__` в `layout.standalone`.
  ⛔ Не `__FONT__`: он префикс `__FONTS__`. Сторож `_INTER` в `test_structure`
  считает `__FAMILY__` вторым ответом на вопрос «просит ли страница шрифт».
  ⚠️ Печатные бланки остаются на Inter: документ не зависит от настройки экрана.

Хранение — `clinic.json › theme.menu / theme.font`; чтение — `theme.current()`
с откатом к умолчаниям. Сохранение: поле не прислали — «не сообщали», прежний
выбор цел (прайор 08-16: старый клиент без этих групп не имеет права их
сбросить); прислали незнакомое — `bad_set`/422 с именем поля.

Экран «Aspect» (React и старая страница) получил две группы карточек:
мини-меню в цвете варианта и «Aa» тем самым набором семейств, что уедет в
`--font` (`style=` с сервера — второй записи набора в CSS нет). Предпросмотр —
переменными сервера инлайном на `<html>`, как у стиля. Стенд `look_sweep.py`
принимает `--menu` и `--font`.

## Ступени (B5, шаг 1 — 25.09.2026)

Стиль темы — по-прежнему набор переменных (`theme.STYLES`), но теперь он
достаёт до ФОРМЫ, а не только до фона и теней. Что стало переменной и почему:

| Группа | Переменные | Было |
|---|---|---|
| радиусы | `--r-card` `--r-block` `--r-ctl` `--r-chip` `--r-s` `--r-xs` | 104 числа в 20 значениях (4…24px) |
| высоты | `--h-ctl` `--h-ctl-s` `--h-ctl-m` `--h-nav` `--h-top` | 44/36/40/44/72 по месту |
| меню | `--side-bg` `--side-fg` `--side-hover` `--side-on` `--side-line` `--side-off` `--side-mark` `--nav-mark` `--nav-fw` `--nav-fw-on` `--sec-fs` `--sec-ls` `--sec-tt` | `--teal-d`, `--on-teal-d`, белый с прозрачностью |
| шапка и слой | `--h-top` `--top-bg` `--field` `--layer-bg` `--layer-line` `--layer-r` | `--panel`, `#FBFDFE`, слоя не было |
| заголовки | `--fs-h1` `--fw-h1` `--ls-h1` `--h1-mark` `--fs-h2` | 32/700/-.02em/планка/18 |
| отклик | `--ring` `--focus-line` `--lift` `--lift-card` `--lift-tile` `--sh-hover` | 11 одинаковых колец, 17 подскоков |
| шрифт | `--font` | `body{font-family:'Inter',…}` |

Ступень радиуса выбирается по РОЛИ элемента (карточка / блок внутри карточки /
контрол / чип / ячейка / бейдж), а не по прежнему числу; пилюля (`999px`) и
круг (`50%`) — форма, не размер, и стиль их не трогает. Три сегодняшних стиля
получили те же значения, что имели: `modern` — байт в байт (`test_theme`
сверяет `STYLES["modern"]` с `:root`), у `elegant`/`calm` элементы-карточки,
которые раньше стояли числом (аватар фиши 18px, документ 16px, плитка списка
20px), теперь следуют их `--r-card` (12/22) — то есть стиль впервые применяется
к ним честно.

⛔ **Под пальцем высоты не уменьшаются.** Стиль может опустить `--h-ctl` для
мыши (Fluent — 32px), но `@media (pointer:coarse)` сразу за `:root` возвращает
44/36/40/44 с `!important`: строка темы печатается в `<head>` ПОСЛЕ panel.css и
иначе выиграла бы порядком. Решение Олега 25.09: 32 мышью, 44 сенсорный.

⭐ **Сторож** — `test_structure` › «радиус берётся ступенью», «подскок при
наведении», «кольцо фокуса» (+ якорь исключения), с мутациями и законными
правками в `mutate.py`: число в `border-radius` от 5 до 99px, `translateY(-N)`
в `:hover`, `0 0 0 3px var(--teal-ring)` вне `.sfbtn.sel` — красное. Смотрит и
`panel.css`, и `frontend/src/app/app.css`.

⭐ **Доказательство «ничего не изменилось»** — `scripts/look_sweep.py`: все
экраны карты × React и `?ui=legacy` × все стили, кадр «после» против «до»
попиксельно (порог 120 px на кадр; время в тексте обнуляется, анимации и часы
маскируются). Перед правкой оформления: `--tag base`; после: `--tag X
--against base`. Шаг 1 дал расхождения только на углах (сглаживание при
смене радиуса на 1–6px) — карты отличий лежат рядом с кадрами.

## Чем она может владеть

```
Button    Input    Select   Checkbox  Radio
Modal     Dialog   Table    Tabs      Card
Badge     Tooltip  Dropdown Toast     Form
DatePicker  Loading  EmptyState  ErrorState
```

Вид, отступы, поведение, состояния, доступность с клавиатуры.

## Чем она владеть НЕ может

Три вещи считаются на сервере, и вторая реализация в TypeScript ломает их молча.

### Тема

Источник — `core/theme.py`, отдаёт готовую строку `:root{…}` через
`vars_css()`, которая печатается в `<head>` **после** ссылки на `panel.css`.

Там не список цветов, а арифметика: восемь переменных из одного hex, подбор
`--teal-d` циклом до contrast не ниже 4.5, порог `--on-teal` = 3.0 (это про
кнопку) и `--on-teal-d` = 4.5.

⚠️ **Почему ошибка невидима.** У цвета текста есть свой фон: `--on-teal`
посчитан под `--teal` и на `--teal-d` врёт. На зелёной теме по умолчанию обе
переменные белые — то есть разработчик не увидит ничего, а клиника со своим
цветом увидит нечитаемый текст. Сторожит `test_theme.py` (41 проверка), и он
перестанет что-либо значить, если палитру начнут считать в TS.

Брать: `var(--teal)`, `--teal-d`, `--teal-ring`, `--teal-glow`; текст на
фирменном фоне — `var(--on-teal)`, на тёмном фирменном — `var(--on-teal-d)`.

⛔ Цвета смысла (`--red`, `--amber`, `--green`, `--blue`) теме **не отдавать**:
фиолетовая «Urgențe» — потеря значения, а не персонализация. По той же причине
палитра зуба (эмаль, розовый кариес, синяя пломба в `teeth_svg.py`) зашита
намеренно.

### Иконки

Источник — `core/layout._I`, 76 штук, 357 вызовов `_ic`. `icons.ts`
**генерируется** скриптом сборки с union-типом `IconName`, а не пишется руками.

⛔ **Ничего графического от Windows.** Эмодзи рисует система: по-разному на 10
и 11, всегда цветными и мимо выбранного клиникой цвета. То же верно для знаков,
которые эмодзи не выглядят: вшитый Inter покрывает четыре подмножества, и в них
**не входят** U+2192, U+25B2..25C0, U+2714/2715, U+23F3, U+2212, U+2248. Они
уйдут в системный шрифт наравне с эмодзи. Разделитель пути в тексте — U+203A,
он в подмножестве.

Значок в интерфейсе = иконка, идущая `currentColor`, поэтому она сама берёт
цвет клиники.

### Тексты ответов

Источник — `core/layout.MSG_BANNER`, 90 кодов. Плюс `STATUS_LABEL`,
`ALERT_KINDS`, `SOURCE_LABEL`.

⛔ Второго словаря в TypeScript быть не должно. Правило нарушалось дважды и оба
раза молча: один статус звался «Finalizat» в повестке дня, «a venit» в списке и
«finalizată» в летописи — регистратура видела не то слово, которое нажимала.

## Настольная, а не мобильная

Постоянный сайдбар, плотные таблицы, клавиатура, нативные диалоги, осмысленные
размеры окна. Не перегружать анимациями.

⭐ Правило анимаций остаётся прежним: выключатель — класс `anim` на `<html>`, и
правило **без** `.anim` обязано оставлять элемент в конечном виде. Статичная
страница — норма, а не поломка.
