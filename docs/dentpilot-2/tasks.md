# Список работ

Конкретные шаги с признаком готовности у каждого. Числа взяты из кода
(`python scripts/screen_map.py`), а не на глаз.

```
ВСЕГО 64 задачи
  A  независимое от миграции      6   ← можно делать хоть завтра
  B  фундамент                    6
  C  экраны                      27   ← основной объём
  D  сторожа                      5
  E  рабочий стол                 8
  F  выпуск                       6
  G  подпись и витрина            6
```

⭐ **Конец виден.** Экранов **25**, а не «сколько получится»: из 41 семь
печатных и восемь аварийных остаются серверными навсегда, один — фрагмент.
Задач в блоке C — 27, потому что журнал разложен на четыре строки, а разбор
909-строчной фиши стоит отдельной.

⚠️ Число задач проверяется, а не помнится:

```bash
grep -c '^- \[ \]' docs/dentpilot-2/tasks.md
```

---

## Правила, чтобы не застрять

1. **Один экран = один коммит = один флаг.** Не начинать следующий, пока
   предыдущий не включён у пилота или явно отложен.
2. **Экран не закрыт, пока его проверки не переписаны.** Зелёный прогон,
   потерявший смысл, хуже красного.
3. **Незапланированную находку не чинить на месте** — записывать строкой в этот
   файл и идти дальше. Иначе один экран съедает неделю.
4. **После каждого блока — полный прогон движка.** Без пайпа, иначе код выхода
   теряется.
5. ⛔ **Блок E не начинать**, пока блок C не закончен. Там точка невозврата.
6. Если задача не двигается два захода — **вынести в отдельную строку
   «отложено» с причиной**, а не бороться.

---

## A. Независимое от миграции

Окупается само по себе. Если всё остальное остановится — это останется.

- [x] **A1. Метаданные exe.** ✅ 18.09 вместе с A3: ресурс VERSIONINFO
      генерируется из `APP_VERSION` (`scripts/sync_version.py`), сборка зовёт
      его первым шагом и отдаёт PyInstaller ключом `--version-file`.
      ⛔ Правленого файла ресурса в репозитории нет намеренно: он был бы ещё
      одним источником версии, который надо помнить обновить.
- [ ] **A2. Тринадцать непокрытых маршрутов.** Список — в
      [screen-test-map.md](screen-test-map.md). Начать с `POST /admin/update/run`:
      он подменяет exe у клиники и не покрыт ничем. ⏳ 17.09: осталось 7 —
      `medici/add`, `medici/colors`, `relink`, `doctor-photo` закрыты
      `test_doctors.py`; `lan/firewall` и страница `backup` — `test_settings_api.py`
      (сам вызов netsh через runas в тестах не делается: UAC). 17.09 вечером: 6.
      ⏳ 18.09 пересчитано генератором, по-прежнему 6, поимённо:
      `POST /admin/lan/firewall` (UAC, кандидат в «осознанно оставленные»),
      `GET /admin/logout`, `POST /admin/settings/crypt/off`,
      `POST /admin/telegram/save`, `POST /admin/update/check`,
      `POST /admin/update/run` — последний подменяет exe у клиники.
      *Готово:* таблица «без единой проверки» пуста или в ней остались только
      осознанно оставленные, с причиной.
- [x] **A3. `scripts/sync_version.py --check`.** ✅ 18.09. Источник —
      `engine.py`; потребители — `frontend/package.json` и ресурс свойств exe.
      Зовётся из `.\dev check` и из прогона (`test_guards.suite_version_source`,
      он же в CI через `run_tests.py`). ⚠️ Правила в `test_structure.py` НЕТ
      осознанно: тот разбирает литералы `bot\`, а здесь сверяются файлы из
      разных миров (py, json, ресурс Windows), и мутация ломала бы его не там,
      где смотрит правило. Сторож краснеет сам — проверка подменяет
      `package.json` на копии и требует находки.
      ⭐ В первый же запуск поймал настоящее расхождение: `package.json`
      отставал на версию.
- [ ] **A4. Копия базы пилота и замеры.** Строки в `patients`, `appointments`,
      `teeth`, `documents`, размер `data\files` и самой базы. Потом время
      ответа `/admin/search` и `/admin/stats` за 366 дней на копии.
      *Готово:* числа записаны; решение по `_pl_rows` принимается по ним.
      ⏳ 17.09: на синтетике (5 000 фиш, 20 000 записей) старая страница
      230–545 мс, `GET /api/patients` 130–200 мс — см. `migration.md`,
      этап 6; копия пилота по-прежнему не снята.
      ⏳ 18.09 Олег: у пилота программу поставит заново — копия его базы не
      приоритет; замеры остаются на синтетике.
- [x] **A5. USB-кит.** ✅ 18.09: в ките `DentPilot-Setup-1.27.0.exe`,
      отставшая 1.21.4 убрана (она цела в `releases\arhiva\`). Инструкцию
      править не пришлось: `PROCHTI_MENYA.txt` версию нигде не называет.
- [ ] **A6. Полная офлайн-копия папки установки пилота.**
      *Готово:* архив лежит вне рабочей машины. ⛔ Это условие для блока E.
      ⏳ 18.09 Олег: пилот переустанавливается заново — копия его папки не
      важна; условие для E остаётся для клиник с данными.

---

## B. Фундамент

- [x] **B1. Флаг экрана в `clinic.json`.** ✅ 17.09: `ui.react`, `layout.react_on`, плюс `?ui=legacy`. Рантайм, не пересборка. Читает
      сервер, решает — отдать старую разметку или `<div id="root">`.
      *Готово:* правка файла переключает экран без пересборки; выключение
      возвращает старый экран.
- [x] **B2. `api_guard` / `api_require`.** ✅ 17.09: `core/api.py`. JSON 401/403 для `/api/*`.
      ⛔ 53 вызова `_guard` и 41 вызов `require` на HTML-маршрутах не трогать.
      *Готово:* `curl` без куки на `/api/*` даёт 401 и `application/json`.
- [x] **B3. Конверт ответа.** ✅ 17.09: `layout.msg_json`, с `text`/`tone` из словаря. `{ok, code, field, data}` с существующими кодами
      `MSG_BANNER`. Новых кодов не изобретать.
      *Готово:* хелпер на сервере, тип на клиенте, оба ссылаются на один список.
- [x] **B4. Генератор `icons.ts`** ✅ 17.09: `scripts/gen_icons.py`, свежесть держит `test_structure`. из `layout._I` (76 иконок) с union-типом.
      *Готово:* `--check` краснеет, если списки разошлись.
- [x] **B5. Первый эндпоинт + первый тест на данных.** ✅ 17.09: `/api/settings/clinic`, `tests/test_api.py`. Любой из группы 1.
      *Готово:* набор проверяет JSON, а не разметку.
- [x] **B6. `Build-Desktop.ps1` зовёт `npm run build`** до PyInstaller. ✅ 17.09, проверено под PowerShell 5.1; `smoke_exe` проверяет бандл.
      ⛔ Делать в том же коммите, где включается первый экран — иначе сборка с
      чистого клона положит в exe страницу без бандла.
      *Готово:* сборка на чистом клоне даёт работающий экран.

---

## C. Экраны — 25 штук

Каждый: эндпоинты → компонент → состояния (загрузка/пусто/ошибка) → валидация
→ права → проверки переписаны → флаг заведён и выключен → включён у пилота.

### Группа 1 — формы и таблицы (15)

Без живого опроса, без печати, без перетаскивания.

- [x] C1 `/admin/settings/clinic` — 21 стр ← **первый** ✅ код 17.09 (экран, состояния, права, 69 серверных + 29 клиентских проверок); ⏳ флаг у пилота выключен
- [x] C2 `/admin/settings/lan` — 6 стр ✅ 17.09 (`settings_lan`); проза страницы приходит с сервера кусками, кнопки React; ⏳ флаг у пилота выключен
- [x] C3 `/admin/settings/faq` — 6 стр ✅ 17.09 (`settings_faq`); вопросы и ответы — `faq.entries()`, одни на обе страницы; ⏳ флаг выключен
- [x] C4 `/admin/casa` — 13 стр ⛔ **остаётся серверным**: это печатный «Raport de casă» со своим `<!doctype>` и `@media print`, лист подписывают ручкой — правило «печать не переносится» (migration-plan). В счёт 25 экранов попал по ошибке; экранов к переносу 24.
- [x] C5 `/admin/settings/backup` — 17 стр ✅ 17.09 (`settings_backup`); выгрузка остаётся обычной формой на старый маршрут (файл потоком), страница теперь покрыта `test_settings_api`; ⏳ флаг выключен
- [x] C6 `/admin/settings/security` — 26 стр ✅ 17.09 (`settings_security`); правила учёток в `_apply_user`/`_drop_user`, смена PIN — `core.auth.change_pin`, одна на форму и API; ⏳ флаг выключен
- [x] C7 `/admin/medici` — 46 стр ✅ код 17.09 (`doctors_list`); ⏳ флаг у пилота выключен
- [x] C8 `/admin/settings/hours` — 60 стр ✅ 17.09 (`settings_hours`); тот же payload, та же `_val_hours`; ⏳ флаг выключен
- [ ] C9 `/admin/settings/crypt` — 66 стр ⚠️ сейчас страница без тестов
- [ ] C10 `/admin/settings/system` — 80 стр
- [x] C11 `/admin/settings` — 86 стр, хаб ✅ 17.09 (`settings_hub`); плитки — `_hub_tiles()` кусками, старая разметка из них же; ⏳ флаг выключен
- [x] C12 `/admin/settings/services` — 115 стр ✅ 17.09 (`settings_services`); та же `_val_services`, врачи галочками вместо строки id; ⏳ флаг выключен
- [x] C13 `/admin/settings/theme` — 135 стр ✅ 17.09 (`settings_theme`); палитры с сервера, логотип через общий `_logo_action`; `test_theme` зелёный; ⏳ флаг выключен
- [x] C14 `/admin/doctor-card/{dk}` — 182 стр ✅ код 17.09 (`doctor_card`); ⚠️ список дня без карточки визита и кнопок статуса — это журнал (C24–C27); ⏳ флаг у пилота выключен
- [ ] C15 `/admin/stats` — 291 стр, право `PERM_MONEY`

Плюс 20 POST-действий этих модулей.

### Группа 2 — пациенты, список (2)

- [x] C16 `/admin/search` — 308 стр ✅ 17.09 (`patients_search`); отбор, порядок и страница считаются базой (`db.patients_rows`), старая страница с `_pl_rows` не тронута; паритет двух выдач держит `test_patients_api.suite_parity`; ⏳ флаг выключен
- [x] C17 `/admin/patient/{pid}/peek` — 106 стр ✅ 17.09; кусок разметки строит `_peek_html`, один на старую страницу и `GET /api/patients/{pid}/peek`; React вставляет его как есть до переезда самой фиши (C18)

### Группа 3 — фиша (3)

- [x] C18 **Разбор `GET /admin/patient/{pid}`** — 909 строк одной функцией,
      15 обращений к базе. ✅ 18.09 (`patient_card`): арифметика — `card.py`
      (функции, не копия), ресурсы API — профиль, шапка/KPI, предупреждения,
      анамнез, план, сальдо и платежи, документы, визиты, летопись, окно
      записи (10), плюс кусок одонтограммы до C21; пин старой страницы —
      `test_patient_card.suite_pin` (44), паритет API — `suite_api` (32);
      ⚠️ попутно: `/api` не опознавал вошедшего — летопись подписывалась бы
      «recepție» (починено в `main._identify`); ⏳ флаг у пилота выключен
- [x] C19 `/admin/visit/{appt_id}` ✅ 18.09 (`visit`): `_visit_ctx`/`_save_visit`
      одни на форму и `GET/POST /api/visits/{aid}`, графы и шаблоны — данными
      из `visit.py`; `test_visit_api` (53); ⏳ флаг у пилота выключен
- [x] C20 Документы, анамнез, платежи, план — действия фиши ✅ 18.09 вместе
      с C18: `POST /api/patients/{pid}/…` (профиль, архив, стирание,
      предупреждения, анамнез, план и переходы, платежи, документы и их
      открытие, запись) через те же правила, что у форм (`_save_profile`,
      `_plan_status`, `_store_doc`, `_erase`…); `suite_actions` (56)

### Группа 4 — клиническая графика (3)

- [x] C21 **Одонтограмма — фундамент** ✅ 18.09 (`odontogram`) по контракту
      [clinical-chart.md](clinical-chart.md): пин, `hit=True` в SVG, API
      модели и действий, React `features/clinical/` (дуга, `Tooth`,
      инспектор, история, мосты со скобкой по refs, вид, молочные, легенда),
      компактная карточка фиши тем же модулем (HTML-кусок ушёл);
      `test_odontogram_api` (64), клиент 9; ⏳ флаг у пилота выключен
- [x] C22 **Одонтограмма — UX** ✅ 18.09: клик по поверхности первичным
      жестом и быстрый цикл по списку сервера; черновик ПО ЗУБУ в `useChart`
      («Nesalvat», Save/Enter, Esc/«Renunță», правка держится за зубом);
      клавиатура только в фокусе карты (стрелки, M O D V L, P сверху);
      контекстное меню правой кнопкой и те же действия в инспекторе;
      брейкпоинты 1365/1099 поверх panel.css; `scripts/odo_shots.py` —
      двенадцать кадров Edge с проверкой геометрии; клиент 16 проверок;
      сервер не тронут. ⏳ флаг у пилота выключен
- [x] C23 `/admin/patient/{pid}/parodontograma` — датированные осмотры, API
      сводки (сервер), таблица с диктовкой (клавиша `b`), тот же `Tooth`
      под таблицей; глубина 0 = «не измеряли»; печать серверная.
      ✅ 18.09 — разбор в `migration.md` › этап 11. ⏳ Сравнение осмотров
      вынесено в отдельный этап (слово Олега 18.09); данные для него API уже
      отдаёт

### Группа 5 — журнал (4), последним

- [x] C24 `/admin/week` ✅ 18.09 (`schedule_week`): правила в `schedule/week.py`,
      `GET /api/schedule/week`, React на классах panel.css, разметка старой
      страницы не изменилась ни на байт. Под флагом неделя НЕ живая, а день с
      тем же ключом `dash` остаётся живым — держит `suite_switch`. Разбор —
      `migration.md` › этап 12.
      ⛔ Ключ `dash` снимать по-прежнему нельзя: он общий с `/admin`, очередь
      которого в C26.
- [x] C25 `/admin/all` и `/admin/doctor/{dk}` ✅ 19.09 целиком: C25.1–C25.5a
      (контракт построителя, пины страниц, вынос правил, паритет, React для
      чтения) и C25.5b (форма записи, диалог «+» с обеими вкладками, карточка
      визита, перенос перетаскиванием). Разбор — `migration.md` › этапы 13–14.
      ⚠️ Список дня, отбор плитки и Excel закрыты отдельным этапом C25.5c
- [x] C25.5c `Lista zilei` ✅ 19.09: список дня, отбор плитки (`?f=`),
      `Șterge`/`Restabilește` у заметки, Excel. Новых маршрутов не
      понадобилось — кнопки списка бьют в тот же `POST …/status`. Разбор —
      `migration.md` › этап 15. **Дневной экран достиг паритета с
      `/admin/all`**, и C26 начинается с законченного workflow
- [ ] C14+ Карточка врача: кнопки статуса в списке сегодняшнего дня. Экран
      перенесён (C14), но его таблица укорочена — без карточки визита и без
      кнопок исхода; старая фиша печатает тот же `_list`. Модель для этого уже
      есть (`core/visits.list_rows`, C25.5c), правка одна.
      ⛔ Отдельная задача, НЕ часть C26: слово Олега 19.09 — то, что нужный
      core-код появился по дороге, не повод прятать работу внутрь чужого этапа
- [~] C26 `/admin` — день, живая сетка. Порядок тот же, что у C25: контракт →
      пин → вынос правил → паритет → React. ✅ **C26.1 (разведка) сделана
      19.09** — контракт страницы и план пинов в
      [admin-contract.md](admin-contract.md). Дальше C26.2 (пины).
      ⛔ **Ключ `dash` в C26 не снимается вовсе**, и это сильнее прежней
      формулировки. Разведка показала три вещи: `dash` — ещё и ключ подсветки
      меню (убрать строку нельзя); ключ носят ДВА экрана, включая легаси-
      неделю (у пилота флаги выключены, и снятие погасило бы её); а React-
      панель перестаёт быть живой САМА — `_shell` смотрит на тело, а не на имя
      ключа. Значит снятие ключа = отнять живой опрос у СТАРОЙ страницы, и
      оно уместно только вместе с её удалением, после пилота и окна отката
      (абсолютное правило 8)
- [ ] C27 Протокол живого журнала **поверх данных**, а не разметки

*(C24–C27 — четыре задачи, но один экранный блок; в счёт 25 входит журнал
целиком.)*

---

## D. Сторожа

Правятся **одним заходом** — `mutate.py` сверяет списки в обе стороны и упадёт
сам, если править по частям.

- [ ] **D1.** Шесть правил `test_structure.py`, разбирающих литералы `.py`.
      ⛔ После ухода разметки в `.tsx` они найдут ноль нарушителей и
      **позеленеют навсегда**.
- [ ] **D2.** `mutate.py` — мутации под новые правила.
- [ ] **D3.** `test_guards.py` — 8 из 12 проверок.
- [ ] **D4.** `smoke_exe.py` — около 10 проверок из 31 привязаны к `panel.css`,
      `panel.js`, `fonts.css`. ✅ Проверка бандла добавлена 17.09; остальное ждёт ухода разметки.
- [ ] **D5.** Тесты клиента: маршрутизация, формы, валидация, состояния,
      отказ API, одонтограмма, пародонтограмма. ⏳ Vitest заведён 17.09, первый экран покрыт (29); дальше — по экранам.

---

## E. Рабочий стол

⛔ **Не начинать, пока C не закончен и A6 не сделан.** Здесь точка невозврата.

- [ ] **E1.** Rust + MSVC Build Tools. ⛔ `CARGO_HOME` и `RUSTUP_HOME` на D:
      **до** установки (на C: около 16 ГБ, тулчейн 3–6 ГБ, `target/` ещё 1–3).
- [ ] **E2.** Окно Tauri на `http://127.0.0.1:<порт>/`. ⛔ Не `tauri.localhost`.
- [ ] **E3.** PyInstaller `--onedir` вместо `--onefile`.
- [ ] **E4.** Три страховки от осиротевшего процесса: onedir, Job Object с
      `KILL_ON_JOB_CLOSE`, поток-сторож на исчезновение родительского PID.
- [ ] **E5.** Надзор за упавшим сайдкаром + **ротация лога** (сегодня её нет,
      цикл перезапусков растил бы файл бесконечно).
      ⛔ Не перезапускать при `db-key.pending` / `db-decrypt.request`.
- [ ] **E6.** Диалоги, загрузки (10 `FileResponse` + 4 `Content-Disposition`),
      single-instance. ⚠️ Плагин **фокусирует** окно, а сегодня второй запуск
      открывает второе — смена поведения, решить явно.
- [ ] **E7.** Правило брандмауэра на путь сайдкара, а не на `sys.executable`.
      Проверять на настоящей Windows.
- [ ] **E8.** E2E через `tauri-driver` + `msedgedriver` совпадающей версии.

---

## F. Выпуск

- [ ] **F1. Эксперимент с подменой.** Собрать заготовку Tauri → назвать
      `DentPilot.exe` → положить вместо `dist/DentPilot.exe` → пройти
      существующий путь обновления → проверить запуск, FastAPI, порт, базу,
      данные клиники, файлы. ⛔ Только после успеха трогать релизные скрипты.
- [ ] **F2.** `tauri build --bundles none` → Inno. ⛔ NSIS не брать.
- [ ] **F3. Установка N → N+1 на чистой машине** с живым пациентом.
      ⛔ Данные исчезли — выпуск запрещён.
- [ ] **F4.** Прогон на всех базах-образцах из `test_migrate.py` +
      `check_slot_guard.py`.
- [ ] **F5.** Обновление на ноутбуке **от батареи** (`DisallowStartIfOnBatteries`
      включён по умолчанию и ключом не снимается).
- [ ] **F6.** CI ступени 2–4. ⛔ Publish остаётся ручным.

---

## G. Подпись и витрина

Идёт параллельно, упирается в бумаги, а не в код.

- [ ] **G1.** IDNO. ⛔ Держит одновременно подпись, сайт и SMS-пакет.
- [ ] **G2.** Сертификат: OV или EV. ⚠️ При двух установках репутация
      SmartScreen у OV не накопится практически никогда — вся ценность подписи
      в **первой установке**, то есть в моменте продажи.
- [ ] **G3.** Подпись `DentPilot.exe` **первым**, после PyInstaller и до
      дымового теста.
- [ ] **G4.** Подпись установщика **вторым**, после Inno. Плюс
      `SignedUninstaller`.
      ⛔ Подписать только установщик — худший вариант: самообновление качает
      `DentPilot.exe` напрямую и молча вернёт клинику к неподписанному файлу.
- [ ] **G5.** Пересъёмка витрины и правка README (678 строк, 8 скриншотов).
- [ ] **G6.** Публикация сайта `dentpilot.md`.

---

## Чего в списке нет намеренно

- **Linux и macOS** — продукт продаётся на Windows.
- **Переписывание движка на Rust** — 23 635 строк работающей и проверенной
  логики.
- **Менеджер состояния** — появится, когда появится причина.
- **SaaS** — дверь открыта архитектурой (`/api/*` и двухбэкендный `db.py`),
  но строить сейчас нечего и не для кого.
- **Обновлятор Tauri, NSIS, MSIX, Store** — разобрано в
  [updates.md](updates.md) и [installer.md](installer.md).

---

## Где можно остановиться и не пожалеть

| После | Что получено |
|---|---|
| **A** | подписанное приложение с честными свойствами файла и залатанными дырами в тестах |
| **B** | JSON API — дверь к React, к мобильному приложению и к SaaS |
| **C группа 1** | половина экранов без перезагрузок, старый интерфейс цел |
| **C целиком** | то самое «полноценное приложение», при прежнем exe и прежних обновлениях |
| **E** | нативная оболочка — ⚠️ наименьший вклад в ощущение и наибольший риск |
