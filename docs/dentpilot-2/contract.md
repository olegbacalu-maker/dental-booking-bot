# Договор с клиникой — пункты юристу (L11, 24.09)

Что отдать юристу: три файла.

1. [`docs/site/termeni.html`](../site/termeni.html) — Termeni și condiții, публичные
   условия на румынском. Это и есть договор: клиника принимает его активацией
   первого файла лицензии (п. 1). Заменяет прежний `termeni.html` сайта целиком.
2. [`docs/site/privacy.html`](../site/privacy.html) § 5 «Datele contului clinicii» —
   что и зачем поставщик хранит о клинике на сервере лицензий.
3. Этот файл — суть по-русски, таблица «обещание ↔ программа» и вопросы.

Опорный язык юридических текстов — румынский, как у сайта. Русской версии
нет намеренно; если юрист попросит, переводится с румынского, а не наоборот.

## Суть по пунктам

| § | По-русски | Термин на странице |
|---|---|---|
| 1 | Поставщик — Олег Бакалу, antreprenor independent (Legea 228/2025), IDNO. Клиника получает неисключительное право пользоваться программой на своих компьютерах на срок пробного периода и абонемента. Условия действуют с активации первого файла. | furnizorul, clinica, fișier de licență |
| 2 | Программа работает локально и от интернета не зависит. Три состояния, выводимые только из дат файла: активна · период оплаты (работает полностью) · режим чтения. | Activ, Perioada de plată, Regim de citire |
| 3 | **Пробный период — первый месяц, 30 дней** (с 26.09; было 14 — сайт с самого начала обещает «Prima lună — gratuită»), без оплаты и обязательств; запрашивается формой на cloud.dentpilot.md (L14) или письмом, один раз на клинику (IDNO / e-mail). После него 3 дня полной работы на оформление абонемента, затем режим чтения. Данные пробного периода остаются. | perioadă de probă de 30 de zile, perioada pentru abonare |
| 4 | Абонемент **на месяц или на год** по цене оферты сайта (499 / 5 489 MDL: год — 11 месячных; решение 26.09, 3 и 6 месяцев нет). Оплата **переводом** по ноте с референсом (референс в назначении — условие признания платежа). Карта maib (L12) в код сервера встроена, но в тексте договора её нет, пока нет договора эквайринга: вернуть фразу про карту на страницу — в день включения `DP_MAIB_*`. Оплаченный период начинается с большей из дат: конец текущего или подтверждение платежа. После подтверждения — новый файл письмом. За 14 дней до конца — нота, за 3 дня — уведомление. | nota de plată, referință de plată |
| 5 | После конца оплаченного периода **14 дней полной работы (период оплаты)**, уведомления в день истечения, накануне и в день перехода в режим чтения. | perioada de plată |
| 6 | **Режим чтения**: данные не теряются и не блокируются. «Datele se pot consulta, tipări și exporta» — дословно баннер программы; картотека, печать, экспорт, бэкап доступны. Новый файл возвращает полную работу немедленно. Поставщик данные не удаляет, не блокирует, не удерживает. | regim de citire |
| 7 | Файл выдан одной клинике (название + IDNO), не передаётся. Другой компьютер той же клиники — тот же файл. Каждое продление — новый файл с бо́льшим номером; программа принимает только файлы поставщика и только новее активированного. Программа с доступом в интернет забирает новый файл сама, раз в сутки; без интернета — активация вручную (L13). Потерянный файл высылается повторно. | fișier de licență, număr de ordine |
| 8 | Обновления включены. Клиники, работавшие до появления файла лицензии, получают **14 дней с первого запуска новой версии** на активацию; всё это время программа работает полностью. | dispoziții tranzitorii |
| 9 | Данные пациентов остаются на компьютере клиники; поставщик доступа к ним не имеет и **persoană împuternicită не является** (Legea 195/2024); оператор — клиника. По данным учётной записи клиники поставщик — оператор в целях исполнения договора. Бэкапы делает клиника средствами программы; у поставщика копий данных клиники нет. | operator, persoană împuternicită |
| 10 | Поддержка по почте и телефону в рабочие дни. Программа не зависит от серверов поставщика. | suport |
| 11 | Ответственность ограничена суммой, уплаченной за последние 12 месяцев; поставщик не отвечает за потерю данных из-за отсутствия бэкапов, поломки компьютера или действий пользователей клиники. | răspunderea |
| 12 | Клиника прекращает в любой момент без штрафов и предупреждения; автопродления и долгов за будущие периоды нет. Поставщик — с предупреждением за 30 дней и возвратом за неиспользованный оплаченный период. При прекращении данные остаются у клиники, программа уходит в режим чтения по истечении оплаченного срока — бессрочно. | încetarea |
| 13 | Изменения условий публикуются на странице, клиники с абонементом уведомляются за 30 дней; оплаченный период не затрагивают. | modificarea termenilor |
| 14 | Право Республики Молдова; переговоры, затем суд. | legea aplicabilă |
| 15 | Контакты поставщика. | contact |

## Обещание ↔ программа

Каждая строка — то, что страница обещает, и то, чем программа это делает.
Числа и фразы не переписаны руками: тесты берут их из кода и сверяют со
страницей (`tests/test_structure.py` — «договор согласован с программой
дословно», `cloud/tests/test_contract.py`).

| Обещание | Программа / сервер | Где |
|---|---|---|
| пробный месяц (30 дней) + 3 дня на оформление | `TRIAL_DAYS = 30`, `TRIAL_GRACE_DAYS = 3` | `cloud/app/license.py` |
| 14 дней полной работы после срока | `GRACE_DAYS = 14`; в файле `grace_until = valid_until + 14` | `cloud/app/license.py`, `payments.confirm` |
| режим чтения: консультировать, печатать, экспортировать | ворота `_recovery_gate` + белый список `READONLY_ALLOW` (картотека, печать, экспорт, бэкап, права по данным) | `bot/app/main.py`, `bot/app/core/license_state.py` |
| «Datele se pot consulta, tipări și exporta» | баннер `license_readonly` теми же словами | `bot/app/core/layout.py` |
| оплаченный период от большей из дат | `extend_from(valid_until, months, now)` | `cloud/app/payments.py` |
| месяц или год; год — 11 месячных | `MONTHS = (1, 12)`, `BILLED`, `config.PRICE_MONTH = 499` | `cloud/app/payments.py`, `cloud/app/config.py` |
| нота за 14 дней, уведомление за 3, в день, накануне, в день чтения | окна `jobs.windows` | `cloud/app/jobs.py` |
| файл одной клинике, только новее активированного | claim с `clinic_id`/`idno`, отказ `license_older` по `seq` | `bot/app/core/rsa_verify.py`, `license_state.py` |
| только файлы поставщика | подпись RSA-3072, `KEYS` в программе | `bot/app/core/rsa_verify.py` |
| 14 дней с первого запуска у обновившейся клиники | `NO_FILE_GRACE = timedelta(days=14)` | `bot/app/core/license_state.py` |
| программа не зависит от сервера | без сети стартует по запомненным датам; сервер не нужен для работы | `license_state.evaluate`, `license.state` |
| новый файл программа забирает сама, раз в сутки | суточный запрос `renew.url` с токеном из файла, замена только на больший `seq`; сервера нет — строка в лог, ничего больше | `bot/app/core/license.py`, `license_renew.py`; `cloud/app/main.py` `/v1/license` |
| картой — на странице maib, платёж признан по подтверждению maib | `payments.settle`: только `OK` из `pay-info` продлевает; callback — сигнал; данные карты сервер не получает | `cloud/app/maib.py`, `cloud/app/payments.py` |
| данные пациентов серверу не передаются | сервер знает только claim: название, IDNO, план, сроки | `cloud/app/license.py` |
| у поставщика нет копий данных клиники | база сервера — клиники, подписки, выдачи, платежи, письма; таблиц с данными пациентов нет | `cloud/app/db.py` |

## Вопросы юристу

1. **Форма договора.** Публичные условия с акцептом «активацией первого
   файла лицензии» (п. 1) — достаточно ли этого для antreprenor independent
   и юридического лица-клиники, или нужен подписанный экземпляр хотя бы для
   первых клиник? Если нужен — та же страница как приложение, подпись на
   одном листе.
2. **Документ на оплату.** Клиника платит переводом по «nota de plată» с
   референсом. Какой документ обязан выдать antreprenor independent по
   Legea 228/2025 при оплате от юрлица: достаточно ли ноты, нужна ли factură
   fiscală или act de prestare? От ответа зависит, что печатает сервер
   (шаг 3 плана).
3. **Возвраты.** Клиника прекращает без возврата за оплаченный период (п. 12.1),
   поставщик — с возвратом за неиспользованное (12.2). Приемлемо ли такое
   несимметричное правило; нужно ли право отказа в первые дни абонемента?
4. **Срок хранения платёжных документов** (privacy § 5) — назвать число лет
   по законодательству о бухучёте вместо «termenul cerut de legislația
   contabilă».
5. **Предел ответственности** суммой за 12 месяцев (п. 11) — работает ли такая
   оговорка по Codul civil между предпринимателями; какие исключения
   обязательны (умысел, грубая небрежность).
6. **Persoană împuternicită.** Формулировка «furnizorul nu are acces la
   datele pacienților și nu este persoană împuternicită» (п. 9.1) должна
   совпадать с бумагой поставщика `clinics/lege-195/3-acord-furnizor.html`
   (приведена 21.09). Оба документа — юристу вместе.
7. **Изменение условий** уведомлением за 30 дней по e-mail (п. 13) — достаточно
   ли, нужна ли оговорка о праве клиники расторгнуть при несогласии.
8. **Язык.** Только румынский; добавить ли «versiunea în limba română
   prevalează» на случай перевода.
9. **maib.** Заключает ли maib договор эквайринга с antreprenor independent
   (вопрос из cloud.md, шаг 2) — не про текст, но спросить в тот же визит.
   ⏳ 26.09: Олег оформляет maib business; до него на странице только перевод.
10. **Обработчики данных учётной записи** (privacy § 5, дописано 26.09):
   хостинг сервера — Contabo GmbH (Германия, ЦОД в ЕС), почта — Google (Gmail).
   Достаточно ли назвать их в политике, нужен ли с ними отдельный договор
   обработки (у обоих — стандартные условия провайдера), и упоминать ли, что
   Google может обрабатывать письма вне Республики Молдова.
11. **IP-адрес из формы `/proba`** хранится вместе с заявкой (защита от
   спама) без срока. Назвать срок хранения — или сервер должен стирать IP
   через N дней?

## Чего в договоре нет намеренно

Автосписания и автопродления (карта — по ссылке, платит клиника, когда
хочет), привязки устройств, SLA по серверу лицензий (программа от него не
зависит), обязательств поставщика по данным пациентов (доступа нет). Оплата
картой (L12) добавляла в п. 4 фразу о платёжном провайдере maib — 26.09 она
снята со страницы до договора эквайринга; остальное не менялось: файл
лицензии и состояния те же.
