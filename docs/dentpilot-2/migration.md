# Миграция — состояние и порядок

Точка отсчёта и полный разбор — [../migration-audit.md](../migration-audit.md).
Что программа умеет **сегодня** — [../current-functionality.md](../current-functionality.md).
Поэтапный план — [../migration-plan.md](../migration-plan.md).
Пошаговый список работ с числами — **[tasks.md](tasks.md)**.

## Доска фаз

| | Фаза | Состояние |
|---|---|---|
| 0 | аудит, базовая линия, копия данных | ✅ аудит и линия · ⛔ копия папки пилота **не снята** (18.09 Олег: пилот переустанавливается заново, копия не приоритет) |
| 1 | CI, только прогон движка | ✅ файл есть · ⚠️ на GitHub ещё **не исполнялся** |
| 2 | JSON API и флаг экрана | ✅ 17.09: `core/api.py`, конверт `msg_json`, флаг `ui.react` в clinic.json, `?ui=legacy` |
| 3 | каркас React | ✅ собирается, движок его отдаёт; Vitest и ESLint заведены 17.09 |
| 4 | первый экран — `Setări clinică` | ✅ код 17.09 · ⏳ у пилота **выключен**, включается флагом |
| 5 | остальные экраны за флагами | ⏳ 19 из 23: врачи (2), настройки — хаб, сеть, справка, часы, услуги, тема, учётки, копия, список пациентов (17.09), фиша пациента с её действиями, дневник визита, **одонтограмма — фундамент и UX**, пародонтограмма, неделя журнала, оба дневных экрана — чтение и взаимодействие (18–19.09); касса оказалась печатью и из счёта ушла; шифрование и обновление — по слову Олега не трогаем |
| 6 | сторожа, мутация, тесты клиента, E2E | ⏳ частично: два новых правила + мутации, тесты клиента по экранам, бандл в дымовом тесте; E2E нет — но три экрана проверяются НАСТОЯЩИМ браузером вне прогона (`scripts\odo_shots.py`, `perio_shots.py`, `day_shots.py`: ввод, геометрия, перетаскивание) |
| 7 | оболочка Tauri и sidecar | ⏳ Rust не установлен |
| 8 | упаковка Inno | ✅ работает, менять не требуется |
| 9 | существующий обновлятор | ⏳ эксперимент с подменой не проведён |
| 10 | pre-release, канарейка, ручной publish | ⏳ |

⚠️ Фаза 3 сделана **раньше** фазы 2, и это осознанно: каркас нужен был, чтобы
проверить ограничение имён файлов сборки на живом сервере, а не на бумаге.

## 17.09.2026 — фундамент и первый экран

Задание Олега (четвёртая редакция, «React migration master prompt») зафиксировало
порядок: фундамент → экраны по одному → Tauri после всех → установщик →
подпись → CI/CD. Он совпадает с планом выше; к Definition of Done экрана
добавились **lint** и **тесты компонентов**.

Сделано одним заходом на экране «Clinica»:

- **API:** `core/api.py` — `api_guard`/`api_require` (401/403 JSON, Origin на
  изменяющих методах), `api_body`; конверт `layout.msg_json` —
  `{ok, code, text, tone, field?, data?}`, где `text`/`tone` берутся из
  `MSG_BANNER` на сервере. `GET`/`POST /api/settings/clinic` в
  `modules/settings/api.py` зовут те же `_val_clinic`/`_finish_cfg`, что форма.
- **Рубильник:** `clinic.json` → `{"ui": {"react": ["settings_clinic"]}}`,
  `layout.react_on`/`react_mount`; `?ui=legacy` отдаёт старую форму на один
  запрос; правка файла руками требует перезапуска (профиль читается при
  старте и при сохранении настроек).
- **Клиент:** `features/settings/ClinicSettingsScreen.tsx` — загрузка / готово /
  пусто (шаблонный профиль) / отказ / нет прав / 401 → вход; `Icon` из
  генерируемого `icons.ts` (`scripts/gen_icons.py`); `Toast` с разметкой
  серверной плашки. Vitest 29 проверок, ESLint (typescript-eslint + хуки).
- **Сторожа:** `test_api.py` (69 проверок на данных), два правила в
  `test_structure` (маршруты `/api/` только через `api_guard`; `icons.ts`
  свежий) с мутациями; `smoke_exe` проверяет бандл; `Build-Desktop.ps1`
  зовёт `npm run build` до PyInstaller; в CI отдельная работа клиента.

⚠️ Первый прогон CI на GitHub (до этих правок) упал дважды одинаково: наборы,
которые читают `dental.db` stdlib-ом сразу после `with Server(dir_=…)`, дали
`disk I/O error` (13, затем 18 из 18), локально — по одному на полный прогон.
Причина в харнессе, не в продукте: сервер гасится TerminateProcess-ом, и
первое открытие базы попадает на ещё не отпущенное отображение `-shm`.
`Server.__exit__` теперь дожидается, пока база откроется (`_settle_db`);
исключение Defender пробовалось и ничего не изменило. Кодировка консоли в
`mutate.py` починена.

⚠️ Известное расхождение со старым экраном: после сохранения через API имя
клиники в сайдбаре обновляется при следующем переходе, а не сразу (старая
форма перезагружала страницу целиком).

## 17.09.2026 — этап 2: врачи

Два экрана за один заход, по короткому плану Олега: список `/admin/medici`
(`doctors_list`) и фиша `/admin/doctor-card/{dk}` (`doctor_card`).

- **Правила вынесены, не скопированы:** `_add_doctor`, `_save_doctor`,
  `_set_services`, `_store_photo`, `_delete_photo`, `_reset_colors`,
  `_orphan_warning`, `_week_cells`, `_service_rows` в `doctors/routes.py`;
  старые обработчики стали обёртками с прежними кодами и редиректами.
- **API:** `modules/doctors/api.py` — `GET/POST /api/doctors`,
  `POST /api/doctors/colors`, `GET/POST /api/doctors/{dk}`,
  `POST …/services`, `POST …/photo` (multipart), `POST …/photo/delete`.
  Отказ проверки 422, спор с состоянием клиники (тёзка, последний активный,
  будущие записи, услуга без врача) 409, коды и тексты те же, что у форм.
- **Узел экрана несёт параметры:** `react_mount(..., params)` → `data-params`
  JSON; клиент не разбирает адрес.
- **Клиент:** `features/doctors/*`, общий `hooks/useLoad`, `components/
  LoadFailed`, `Avatar`; `api.postForm` для multipart. 19 проверок Vitest.
- **Тесты сервера:** `test_doctors.py` — три старых POST без единой проверки
  (`medici/add`, `medici/colors`, `relink` с сиротской записью) плюс API и
  рубильник; непокрытых маршрутов осталось 9 из 13.

⚠️ Расхождение со старой фишей: список сегодняшних визитов — таблица со
ссылками на фишу пациента, без карточки визита и кнопок статуса. Это
механика журнала, она переедет вместе с ним (C24–C27), а не по одному экрану.

## 17.09.2026 — этап 3: настройки, пачка A

Хаб, «Acces din rețea», справка и часы работы — четыре экрана одним заходом,
по порядку Олега (LAN, FAQ, хаб, часы), без правок фундамента.

- **Хаб:** плитки стали данными (`_hub_tiles()`): адрес, иконка, тон, подпись
  и строка состояния кусками (текст / иконка с текстом и тоном / точка
  цвета). Старая страница собирается из них же (`_hint_html`), байт в байт.
- **Сеть:** проза страницы разложена в `lan.py` на куски (`intro_html`,
  `status_html`, `firewall_html`, `tips_html`); переключение — общий
  `_set_lan`, перезапуск — тот же `restart_app`, текст перезапуска — константы
  `RESTART_*`, одни на страницу и на JSON. Кнопка брандмауэра в тестах не
  нажимается (UAC), проверяется только охрана.
- **Справка:** `faq.entries()` — записи данными (имя иконки, вопрос, ответ
  HTML-ом), `render()` рисует из них.
- **Часы:** тот же payload, что слал скрипт страницы, та же `_val_hours`.
- **API:** `GET /api/settings/hub`, `GET/POST /api/settings/lan`,
  `POST /api/settings/lan/firewall`, `GET /api/settings/faq`,
  `GET/POST /api/settings/hours`. Две секции отдают HTML-куски (ответы
  справки, проза сети) — это текст сервера из тех же строк, что у старой
  страницы, не отрисовка страницы через API: кнопки и состояния рисует клиент.
- **Тесты:** `test_settings_api.py` — 5 наборов; старые наборы настроек, сети,
  ролей, темы и перезапуска зелёные после выноса. Клиент: 13 проверок Vitest.

⚠️ Расхождение: на старой странице сети блок «правило брандмауэра» содержит
кнопку внутри рамки; в React кнопка стоит под рамкой (проза с сервера, кнопка
своя). После переключения доступа старая страница показывала отдельный
экран перезапуска, React показывает тот же текст плашкой на месте.

## 17.09.2026 — этап 4: настройки, пачка B

Услуги и вид клиники. Касса из пачки выпала: `/admin/casa` — печатный «Raport
de casă» (свой `<!doctype>`, `@media print`, подпись ручкой), и по правилу
плана печать остаётся серверной; в `tasks.md` C4 помечен так, счёт экранов
к переносу — 23.

- **Услуги:** `GET/POST /api/settings/services`. Та же `_val_services`: она
  ждёт врачей строкой через пробел и длительность строкой, как слал скрипт
  старой таблицы, — API приводит список id и число к тому же виду до
  проверки. В React врачи услуги — галочки с именами вместо строки id
  (данные те же: пусто = все). Двуязычная цена, как и у старой формы,
  сохраняется строкой RO — поведение сохранено намеренно.
- **Вид клиники:** `GET/POST /api/settings/theme`, `GET …/theme/palette`
  (палитра своего цвета — считает сервер), `POST …/theme/logo` (multipart),
  `POST …/theme/logo/delete`. Логотип — общий `_logo_action`, вынесенный из
  обработчика формы; `_val_theme` без изменений. Предпросмотр в React ставит
  переменные `:root` из палитр сервера, как делал скрипт старой страницы.
- **Тесты:** `suite_services`, `suite_theme` в `test_settings_api.py`;
  `test_theme` (41 проверка) зелёный на старой странице. Клиент: 8 проверок.

⚠️ Расхождение: у старой таблицы услуг дубли подписей отбивал ещё скрипт в
браузере (иначе форма перерисовывалась и правки пропадали); в React правки
живут в состоянии экрана, поэтому достаточно отказа сервера — правило одно.

## 17.09.2026 — этап 5: настройки, пачка C (учётки и PIN, копия)

- **Смена PIN** жила обработчиком в `main.py`, а модуль не имеет права
  импортировать точку входа. Правило переехало в `core.auth.change_pin`
  (туда, где уже живут `verify_pin`, `_write_pin`, `pin_free`), обработчик
  формы стал обёрткой. Это единственная правка `core/auth.py` за всю
  миграцию, и она добавляет функцию, не меняя существующих.
- **Учётки:** правила `users_save`/`users_delete` вынесены в `_apply_user` и
  `_drop_user`; список разрешённых сравнений роли в `test_structure`
  (`_ROLE_OK`) переведён с `users_save` на `_apply_user`.
- **API:** `GET /api/settings/security`, `POST /api/settings/users`,
  `POST /api/settings/users/{uid}/delete`, `POST /api/settings/pin` (ответ
  несёт свежую куку — ключ подписи вращается), `GET /api/settings/backup`.
  Отказ проверки 422; чужой PIN, последний директор, своя учётка,
  блокировка — 409.
- **Копия:** сама выгрузка остаётся `POST /admin/backup/export` обычной
  формой: архив может весить сотни мегабайт, браузер качает ответ формы
  потоком, а отказ сервер возвращает редиректом на страницу с `?msg=`.
  React-экран отдаёт только форму с порогом пароля с сервера.
- **Тесты:** `suite_security` (в том числе кука после смены PIN в той же
  вкладке) и `suite_backup`; старые наборы PIN, ролей, подбора,
  сигнализации и ревью входа зелёные после выноса. Клиент: 8 проверок.

## 17.09.2026 — этап 6: список пациентов (Search)

- **Сначала пин.** Поведение `_pl_rows`/`_pl_filter` записано руками в
  `test_patients_api.suite_parity` на картотеке с каждой веткой статуса,
  долгом и авансом, врачом из фиши и врачом последнего визита, вторым
  номером в заметке, датой рождения и диакритикой: порядок по умолчанию
  (последний визит, потом никогда не бывшие по id), по имени, по долгу,
  фильтры статуса (включающие), врача («-» = без врача), канала, сальдо,
  скрытый архив и его счётчик, варианты фильтров.
- **`GET /api/patients`** считает отбор, порядок и страницу в базе:
  `db.patients_rows` — ОДНА выборка: те же агрегаты, что стояли по одному
  (`patients_visits`, `…_alert_counts`, `…_plan_counts`, `patients_debt`),
  соединённые с `patients`, врач последнего визита — подзапросом по
  индексу (оконная функция `patients_last_doctor` по всей таблице стоила
  ~50 мс на каждый запрос), счёт отбора — `COUNT(*) OVER ()` в тех же
  строках (второй проход ради COUNT стоил столько же, сколько страница);
  `WHERE`/`ORDER BY`/`LIMIT` — базой. Замер на 5 000 фиш и 20 000 записей
  (harness, тот же ПК): старая страница 230–545 мс, API 130–200 мс,
  сводка 82 мс; остаток — агрегат визитов по всей таблице записей, он же
  у старой страницы. Свёртка диакритики и «только цифры» в SQLite — функции
  `dp_fold`/`dp_digits`, зарегистрированные на соединении в `db.init`
  (тот же `fold`, что у Python-фильтра); у PG — `translate(lower())` и
  `regexp_replace`. Статус строки выводит та же `_pl_status`, что рисует
  старая страница; в SQL только включающие предикаты фильтра.
- ⚠️ **Правило отбора живёт в двух местах** — Python старой страницы и SQL
  API — осознанно: старая страница по слову Олега не меняется до конца
  миграции. Расхождение краснеет в `suite_parity` (39 отборов, строка в
  строку: те же id в том же порядке, те же статусы, тот же счёт). С уходом
  старой страницы уйдут и одиночные выборки `_pl_rows`.
- **Вынесено, не скопировано:** разбор строки запроса `_pl_terms` (зовёт и
  `_pl_match_q`), подпись первой карточки `_pl_new_foot`, кусок
  предпросмотра `_peek_html`, правило новой фиши `_new_patient`. Старые
  обработчики стали обёртками, их наборы зелёные.
- **API:** `GET /api/patients?q=&med=&st=&ch=&dat=&sort=&page=&per=`
  (строки с датами уже в поясе клиники, `total/page/pages`, `hidden_arh`),
  `GET /api/patients/summary` (карточки с подписями-кусками, врачи и
  каналы фильтра, статусы с классами, варианты `per`, врачи диалога — один
  раз на открытие экрана), `GET /api/patients/{pid}/peek` (`{html}`, 404),
  `POST /api/patients` (`new_pat`/`dup_pat` с адресом фиши, 422 `bad_pat`).
  Чужие `st`/`ch`/`dat`/`sort`/`per` откатываются к «все»/«по умолчанию»,
  `page` за пределом — последняя, как на старой странице.
- **React `patients_search`:** та же разметка `.pl-*`/`.pp-*` из panel.css;
  буква в поиске ждёт 250 мс, выбор в фильтре идёт сразу, страница
  сбрасывается; адрес страницы повторяет отбор (`history.replaceState`,
  те же имена параметров — `?ui=legacy` и перезагрузка открывают тот же
  список); заголовки и страницы — настоящие ссылки с перехватом клика;
  предпросмотр — кусок сервера в боковой панели, Escape закрывает; диалог
  «Pacient nou» уводит по адресу сервера. `sessionStorage` для панели не
  нужен: страница не в `LIVE_RELOAD`, перезагрузок нет.
- **Экспорт** остаётся `GET /admin/patients.xlsx` с тем же отбором
  (файл — ответ обычной ссылки), как копия в пачке C.
- **Тесты:** `suite_parity` (137), `suite_api` (39), `suite_switch` (13);
  клиент — 12 проверок экрана и разбора отбора.

## 18.09.2026 — этап 7: фиша пациента (C18 + действия C20)

Самый большой экран до медицинской графики: `GET /admin/patient/{pid}` —
909 строк одной функцией, 15 выборок, вся арифметика внутри f-строк. По
слову Олега — не переносить целиком, разобрать по ресурсам, сперва пин.

- **Сначала пин.** `test_patient_card.suite_pin` записывает старую фишу
  руками на пациенте с каждой веткой: четыре визита (прошлый завершённый с
  дневником, отменённый, прошлый подтверждённый без дневника, будущий),
  план в четырёх статусах с просроченным сроком и позицией без цены,
  платёж и возврат, четыре предупреждения (в шапку идут три), анамнез с
  галочками и свободным текстом, три документа трёх видов, имплант; плюс
  пустая фиша, «achitat integral», аванс, архив, план из одних отказов.
  Заодно найдено: строка `meta` с «arhivat» в обработчике — мёртвый код
  с 1.19.0, на экране архив виден только пилюлей; API повторяет экран.
- **Арифметика вынесена в `modules/patients/card.py`** функциями, а не
  скопирована: `plan_view` (порядок, счёт, вкладка по умолчанию, суммы,
  прогресс без отказов), `visits_view` (следующий/последний/живые/
  отменённые), `pills`, `sold_view`, `anamneza_view` (риски — галочки И
  свободный текст), `consult_kind`, `doc_view`/`doc_size`, `days_ago`,
  `channel`, `appoint_view`; словари плана, документов, mime и имена
  значков — там же (в `routes.py` остались псевдонимы). Старая страница
  зовёт их и рисует прежний HTML — пин и все наборы фиши зелёные после
  выноса. Правила действий вынесены из POST-обработчиков в `_save_profile`,
  `_appoint`, `_free_slots`, `_add_alert`, `_save_anamneza`, `_add_plan`,
  `_plan_status`, `_plan_del`, `_add_pay`, `_store_doc`, `_drop_doc`,
  `_open_doc`, `_erase` — обработчики форм стали обёртками.
- **API:** `GET /api/patients/{pid}[?views=1]` — фиша одним конвертом
  (профиль, пилюли и цифры шапки, предупреждения, анамнез, план,
  сальдо и платежи, документы, визиты — история и живые для списков за
  цифрами, летопись, окно записи, справочники форм); открытие пишется в
  журнал доступа, как у страницы. Действия: `POST …/profile`, `/archive`,
  `/erase`, `/alerts`, `/alerts/{aid}/delete`, `/anamneza`, `/plan`,
  `/plan/{iid}/status`, `/plan/{iid}/delete`, `/payments`,
  `/payments/{id}/delete` (PERM_MONEY), `/documents` (multipart),
  `/documents/{id}/delete`, `/appoint`; `GET …/slots`, `GET …/activity`
  (лента отдельно — переключение «accesările» без второй записи о
  просмотре), `POST /api/documents/{id}/open` (`opened`/`reason`, 200
  всегда: отказ движка = клиент качает файл). Отказ проверки — 422 с
  полем, спор с состоянием (ребро плана, удаление начатой позиции,
  выключенный врач, прошедший час, занятый слот) — 409, удача — свежая
  фиша целиком: почти каждое действие двигает несколько секций сразу.
- ⚠️ **Находка не про React:** движок опознавал вошедшего только под
  `/admin` (`main._identify`), поэтому любое действие через `/api` подписало
  бы летопись «recepție» и спрятало директорские кнопки. Опознание
  распространено на `/api`; держит `suite_actions` («летопись подписана
  именем вошедшего»).
- ⚠️ **Точка интеграции до C21:** компактная одонтограмма едет куском
  старой страницы (`GET …/teeth` = тот же `odontogram.card`: обе дуги,
  оба вида, подсказка, диалог зуба, мосты). Клиент вставляет кусок и
  пересоздаёт его `<script>` по порядку (innerHTML их не исполняет);
  форма зуба остаётся формой старого маршрута и возвращает страницу с
  `?msg=` — плашку рисует сервер. Кнопка зуба в плане зовёт `openTooth`
  куска, а без него ведёт на детальную страницу.
- **React `patient_card`:** та же раскладка (`.hero`, `.kpi5`, `.pv2`,
  `.fcard`, `.plan-row`, `.pay-row`, `.doccard`, `.acti`, `.tline` из
  panel.css), четыре диалога (запись, список за цифрой, просмотр
  документа, отказ); одна функция `act` на все карточки — удача подменяет
  фишу и показывает плашку сервера, отказ отдаёт ошибку форме (поле).
  Черновики форм привязаны к фише, с которой начаты (правило хуков
  запрещает setState в эффекте): свежая фиша после удачи даёт форме свои
  значения, отказ ввод не трогает. `?views=1` — параметр узла и адрес
  через `history.replaceState`. Инлайновые стили старой страницы — классы
  `dp-*` в app.css.
- **Тесты:** `suite_pin` (44), `suite_api` (32), `suite_actions` (56),
  `suite_switch` (14); клиент — 14 проверок экрана.

⚠️ Расхождения со старой фишей: значки способов оплаты внутри `<option>`
не рисуются (браузер и раньше показывал только слово); черновик правки
профиля сбрасывается любым другим действием фиши (старая страница
перезагружалась целиком — то же поведение); после сохранения зуба через
кусок одонтограммы страница перезагружается, как раньше.

## 18.09.2026 — этап 8: дневник визита (C19)

Маленький экран после монолита: одна страница, один POST, рисование уже
жило отдельно в `visit.py`, а `test_visit.py` держал правила записи.

- **Пин** (`test_visit_api.suite_pin`) — то, чего в `test_visit` не было:
  шапка с комментарием стойки, адрес возврата (только журнал, чужой →
  фиша), шесть шаблонов по порядку и правило «заполняет только пустое»,
  графы по порядку бланка, открытые позиции без отказа и с «în lucru»,
  строка авторства с «actualizat», страница без формы у отменённого
  (запись для чтения) и неявившегося (ничего).
- **Вынесено:** `_visit_ctx` (визит, запись, план — или None для заметки),
  `_visit_back`, `_save_visit` (bad_vst / bad_visit / ok_visit, финализация
  отмеченных активных позиций на визите); обработчики — обёртки. В
  `visit.py` графы и шаблоны стали данными (`fields()`, `templates()`),
  текст позиции — `item_text()`, предупреждение — `READONLY_NOTE`: одни
  строки на страницу и на API.
- **API:** `GET /api/visits/{aid}?back=` (визит, запись или null,
  `editable`/`note`, графы, шаблоны с текстами, план — выполненное на
  визите и открытое для отметки, `back`), `POST /api/visits/{aid}`
  (графы + `done` + `back`; 409 bad_vst, 422 bad_visit, удача — свежая
  страница).
- **React `visit`:** `features/visits/VisitScreen.tsx` — те же слова и
  классы (`.vwrap`, `.tplrow`, `.vchk`, `.vdone`, `.vmeta`, `.vsec`
  переехали из `<style>` страницы в app.css под `.dp-react-root`); шаблон
  заполняет только пустые графы; черновик привязан к странице. Параметры
  узла — `aid` и уже проверенный сервером `back`.
- **Тесты:** `suite_pin` (16), `suite_api` (27), `suite_switch` (10);
  клиент — 6 проверок.

⚠️ Расхождение: `?ui=legacy` со страницы React теряет `?back=` (ссылка
заглушки несёт только адрес) — «Înapoi» ведёт в фишу, как без параметра.

## 18.09.2026 — этап 9: одонтограмма, фундамент (C21)

По контракту [clinical-chart.md](clinical-chart.md): сервер владеет
клинической истиной и геометрией, React — композицией и интерактивом.

- **Пин** (`test_odontogram_api.suite_pin`): карта зубов `TEETH` (состояние,
  поверхности и их состояния, отметки, врач, заметка, дата, челюсть и
  сторона зеркала), история `THIST`, подпись кнопки, мосты с ролями,
  подъём дуги в виде сверху, легенда, раскрытие молочного ряда, выбор из
  адреса — одни данные у карточки фиши и детальной страницы.
- **SVG адресуем:** `teeth_svg.surface_hits` — пять невидимых целей с
  `data-s` внутри той же зеркалящей обёртки, по флагу `hit=True` (лицевой
  вид — круги на местах меток, сверху — зоны `occ_zones` в контуре).
  Старые страницы флаг не просят, `test_teeth` не изменился.
- **Данные вместо `<script>`:** `odontogram.tooth_info` (словари; `tooth_data`
  — та же пара JSON-строками для старых страниц), `tooth_title` (подпись —
  одна на title кнопки и API), `legend_items`, `BRIDGE_MATERIALS` (пресеты
  диалога, одни на страницу и API), `model()` — вся одонтограмма для React.
- **Правила действий вынесены:** `_save_tooth` (с `_sf_letters`/`_sf_map`
  по белым спискам — одно место разбора на диалог фиши, инспектор и API),
  `_add_bridge`, `_del_bridge`; обработчики форм — обёртки.
- **API:** `GET /api/patients/{pid}/odontogram` (зубы с рисунками обоих видов
  и целями, история, дуги и подъём, мосты, легенда, словари, врачи),
  `POST …/teeth/{n}` (намерение явным полем: `surfaces` — карта или буквы,
  `marks` — список, отсутствие = «форма не сообщала»; 422 bad_card),
  `POST …/bridges` (422 bad_punte, 409 dup_punte), `POST …/bridges/{id}/delete`
  (404 чужой). Удача — свежая модель. `GET …/teeth` с HTML-куском удалён:
  точка интеграции C18 закрыта.
- **React `features/clinical/`:** `Tooth` (серверный SVG + слои: поверхности
  по `data-s` делегированием, выбор, номер со стороны корней, подпись),
  `DentalArch` (ряды, разделитель, молочные, легенда; показывается ОДИН вид
  — переключает состояние, не CSS), скобки мостов по кнопкам ряда после
  раскладки (`useLayoutEffect` + ResizeObserver, `.br-room` в разметке до
  замера), `ToothForm` (одна на инспектор и диалог; ключ по зубу и счётчику
  сохранений), `ToothInspector`, `ToothDialog`, `ToothTip`, `BridgeTool`
  (бар + диалог ролей), `ViewSwitch` (localStorage тем же ключом),
  `useChart` (выбор зуба и поверхности, действия). Экран `odontogram`
  (`OdontogramScreen`, рамка с узким сайдбаром, зуб из `?t=`) и компактная
  `OdontogramCard` в фише — тот же модуль; после записи зуба фиша
  перечитывает себя тихо (пилюли и летопись зависят от зубов).
  ⚠️ Тихо не было: перечитывание шло GET-ом фиши, то есть открытием, и
  каждое сохранение зуба писало в журнал доступа «Fișa deschisă». С 24.09
  свежая фиша едет в ответе самой записи (`?card=1`, `api.md`).
- **Подписи поверхностей:** ключи M/O/D/V/L, у верхней челюсти L показан как
  P (palatinal) — `surfaceLetter`/`surfaceName`.
- **Тесты:** `suite_pin` (17), `suite_api` (37), `suite_switch` (10); клиент —
  9 проверок экрана (в том числе клик по цели поверхности внутри рисунка,
  роли моста, память вида) плюс обновлённые проверки фиши.

⚠️ Расхождения со старой страницей: в дереве только видимый вид (старая
возила оба и прятала CSS); диалог зуба в фише получил состояние на каждую
поверхность (как инспектор), а не только буквы; после записи зуба из фиши
страница не перезагружается. C22 (UX) — этап 10 ниже.

## 18.09.2026 — этап 10: одонтограмма, UX (C22)

Состав и правила приняты Олегом до начала — [clinical-chart.md](clinical-chart.md)
› C22. Без новых клинических состояний, без правок сервера (в `bot/` не
изменилось ничего, серверные наборы те же), без редизайна.

- **Черновик в `useChart`, не в форме.** Зуб правят ТРИ места — форма, клик
  по поверхности на рисунке и контекстное меню, — поэтому черновик живёт в
  хуке картой по зубу `{base, d}`: `base` — каким зуб был в модели, когда
  правку начали. Клик по соседу правку не теряет (зуб на дуге помечен
  янтарным кольцом `.dirty`, вернёшься — черновик на месте), удачная запись
  меняет `base` — черновик отпадает сам, без счётчика сохранений и без
  эффекта; отказ сервера модель не трогает — ввод остаётся. `ToothForm`
  стала контролируемой (`draft`/`onEdit`), перемонтирование по ключу ушло.
  «Nesalvat» и «Renunță» рядом с «Salvează»; закрыть диалог в фише = сброс.
- **Быстрый цикл:** `pickSurface` — первый клик по поверхности выбирает
  зуб и поверхность, повторный по УЖЕ выбранной крутит состояние по
  `surface_states` сервера: «—» → carie → obturatie → «—» (`cycleState`).
  Работает на дуге и на рисунке инспектора; в фише — нет, диалог модальный.
- **Клавиатура** — обработчик на контейнере `.odop` (tabIndex): стрелки —
  `neighbour()` (ряд / та же позиция другой челюсти, молочные в своей паре),
  фокус переезжает на кнопку зуба; M O D V L — поверхность, P — только на
  верхней; Enter — `save()` черновика, Esc — сброс (в режиме моста — выход
  из него). ⛔ Внутри input/textarea/select и при открытом `dialog[open]` —
  ничего; с Ctrl/Alt/Meta — ничего. Enter и стрелки гасятся, иначе Enter на
  кнопке зуба в фокусе кликнул бы её же. Зуб из `?t=` получает фокус сразу.
- **Контекстное меню** `ToothMenu` (правая кнопка по зубу): состояния
  сервера с отметкой текущего (из черновика) → в черновик, не в базу;
  «Punte nouă de la acest dinte» — режим моста с этим зубом; у молочных
  моста нет; в режиме моста меню не открывается. Те же действия в
  инспекторе: список состояния и кнопка моста от зуба.
- **Отзывчивость** поверх panel.css (её порог 1100 остаётся): ≤1365 —
  инспектор под дугой, ≤1099 — цели поверхностей крупнее (`r: 7.5px` —
  геометрия SVG как CSS-свойство, Chromium/WebView2) и кнопка зуба шире.
- **Кадры Edge** — `scripts/odo_shots.py` (системный Python с
  websocket-client, не CI): двенадцать сцен настоящим вводом через CDP
  (`Input.dispatch*`, не dispatchEvent) и проверки геометрии в странице:
  зубы по порядку без наложений, номер внутри своей кнопки, инспектор не
  пересекает дугу (справа ≥1366, ниже — под ней), скобка по своим зубам у
  своего ряда, выбранная поверхность = подсветка кнопки, без горизонтальной
  прокрутки, меню в окне, консоль чистая. Кадры — `build/shots/odo/`.
- **Тесты клиента:** 16 (цикл, `neighbour`, клавиатура, Enter/Esc с полем
  ввода и открытым диалогом, черновик за зубом, меню и мост от зуба).

⚠️ Намеренно не сделано: подтверждение при уходе со страницы с черновиком
(новая функция), цикл в компактной карточке фиши, цифровые клавиши, клавиши
внутри меню кроме Esc.

## 18.09.2026 — этап 11: пародонтограмма (C23)

По контракту [clinical-chart.md](clinical-chart.md) › C23. Клиническая модель
осмотра не менялась ни на поле: шесть точек на зуб, PD, рецессия, BOP,
подвижность и фуркация живут в базе с 08-25 (v1.26.0) ровно в той форме, в
какой их просил план, — `perio_exams` + `perio_teeth`. Перенос, не редизайн.

- **Пин** (`test_perio_api.suite_pin`, 17): состав листа и подзаголовок,
  строка осмотра в списке («дата · N dinți»), скрытые поля формы (`exam`,
  `covers`, `rev`), ПОРЯДОК двенадцати полей в колонке (глубина V, рецессия V, номер,
  рецессия O, глубина O), значения зуба по местам, точка кровоточивости ровно
  у глубины и ровно шесть на зуб, пометка кармана на клетке, подпись точки
  словами и `aria-label` с номером зуба, степени 0–3 с прочерком на нуле,
  приглушённые отсутствующие зубы и подсказка к ним, только постоянные зубы
  обеими дугами, четыре числа итога справа, снимок врача и заметка, экран
  «осмотров ещё нет».
- **Вынесено в правила** (`routes.py`): `_perio_doctor` (снимок лечащего:
  None — «не сообщали», пустая строка — «стереть», чужое имя — пусто),
  `_save_perio` (осмотр по id + `chart_norm` + `covers` + отпечаток), `_perio_del`;
  обработчики форм стали обёртками. В `perio.py` добавлены `cal()` (CAL по
  шести точкам, только у измеренных), `cal_mean` в `summary()`, `exam_view`,
  `site_items()` и `model()` — данные для React.
- **API:** `GET /api/patients/{pid}/perio[?exam=]` (осмотры со счётом зубов,
  выбранный осмотр, измерения с CAL, зубы с рисунком и пометкой «отсутствует»,
  дуги, шесть точек словами, сводка, пороги, степени, врачи),
  `POST …/perio/exams` (новый осмотр), `POST …/perio/{eid}` (измерения:
  `teeth` картой тронутых зубов, `covers` явным списком, `rev` отпечатком,
  `doctor`/`note` — отсутствие поля значит «не сообщали»),
  `POST …/perio/{eid}/delete` (пустой — 200, с
  измерениями — 409 bad_perio_del, чужой осмотр — 404 bad_perio). Удача —
  свежая карта целиком.
- ⚠️ **Разбор ОДИН на форму и на API.** JSON собирается в ту же проволочную
  строку, что шлёт старая форма, и разбирается `tsvg.parse_perio`. Первая
  версия звала `pack_perio` — и та ЗАЖИМАЛА значение в диапазон: «99» через
  API стало бы карманом 15 мм там, где та же опечатка в форме даёт «не
  измеряли». Поймано проверкой про мусорный ввод.
- **React `features/clinical/`:** `perio.ts` (типы, вызовы, `summarize` —
  предпросмотр итога теми же формулами, что у сервера), `usePerio`
  (черновик ОСМОТРА целиком, коробка по id осмотра с `base`),
  `PerioScreen`, `PerioTooth` (колонка зуба + `Tooth` под ней), `PerioSummary`
  (пять плашек). Классы — те же, что у старой страницы; новых правил в
  app.css четыре (`.ptpic`, `.pexam` и два про печать/номер).
- **Диктовка** — главное на этом экране: цифра уходит к следующей точке, как
  только не может вырасти («1» ждёт вторую, бывает 12 мм), «b» ставит
  кровоточивость с того же места, Enter — следующая точка. ⛔ Enter здесь НЕ
  сохраняет, в отличие от одонтограммы: руки ассистента не уходят с цифровой
  клавиатуры, а осмотр записывается одной кнопкой на весь лист.
- **Черновик по осмотрам:** переключился на прошлый и вернулся — ввод на
  месте (старая страница теряла его перезагрузкой). Удачная запись снимает
  черновик сменой `base`, отказ сервера ввод не трогает.
- **Кадры Edge** — `scripts/perio_shots.py` (системный Python, не CI;
  механика общая с `odo_shots`, там же появился параметр порта): двенадцать
  сцен настоящим вводом через CDP и проверки геометрии листа — 32 колонки по
  12 полей, 192 точки кровоточивости и ни одной у рецессии, номер и рисунок
  внутри своей колонки, сводка не накрывает лист, страница не едет вбок
  (прокрутка живёт внутри `.pscroll`), консоль чиста. Кадры —
  `build/shots/perio/`.
- **Тесты:** `suite_pin`, `suite_api`, `suite_switch`, `suite_day` (дата в
  зоне клиники, без сервера) и `suite_two_seats` (известная дыра, см. ниже);
  клиент — проверки экрана, диктовки, черновика и округления. Счёт — у
  самого прогона, по памяти не ждать.

⚠️ Расхождения со старой страницей, все намеренные:
- пятая плашка итога — средний CAL (старая страница считала его вычислимым и
  объясняла словами, но нигде не показывала; число сервер уже считал);
- печать берёт ВЫБРАННЫЙ осмотр — у старой страницы ссылка вела на печать
  свежего, и распечатать прошлый осмотр было нельзя;
- пустой осмотр можно снять кнопкой: маршрут и сообщение были с 08-25, а
  кнопки не было ни одной, и двойной клик по «Examen nou» оставлял мусор
  навсегда;
- миллиметры на экране всегда с одним знаком (сервер отдаёт 3.0 числом, и
  JSON превратил бы его в «3 mm» там, где печать пишет «3.0 mm»);
- номер зуба на рисунке под колонкой скрыт — он уже есть у самой колонки.

⚠️ Намеренно НЕ сделано: сравнение осмотров (Олег 18.09 — «отдельным
этапом»; API уже отдаёт список осмотров со счётом зубов, так что этап не
упрётся в данные), индекс налёта (его нет в клинической модели — это новые
данные, а не перенос), подтверждение при уходе со страницы с черновиком.

**Что нашло ревью этапа и что с этим сделано** (шесть искателей по
измерениям, по скептику на находку; все девять вердиктов — «подтверждено»,
опровергнутых нет):

| Находка | Решение |
|---|---|
| Ввод, набранный ПОКА идёт запись, пропадал: коробка черновика отпадала на ответ сервера | Черновик теперь ПЕРЕСАЖИВАЕТСЯ на записанный осмотр (`rebase`), а не выбрасывается: записанное перестаёт быть правкой само, набранное после — остаётся «Nesalvat» |
| Заметка с пробелом по краям: сервер её обрезает, и лист считался несохранённым навсегда | При пересадке врач и заметка берутся у сервера — он их и нормализует |
| `Math.round` против питоновского round: BOP% и средняя глубина прыгали на единицу в момент записи (ровно на половинках, 12.5 % → 13 против 12) | `roundLikeServer` округляет половину к чётному — как сервер; сервер не трогали, его числа уходят в печать |
| Смена осмотра: лист висел прежним весь запрос, и цифра уходила в покинутый осмотр | `useLoad.reset()` — экран уходит в загрузку до запроса (как перезагрузка старой страницы) |
| «Examen nou» не менял адрес: F5 возвращал в прошлый осмотр | Адрес синхронизируется одним местом (`syncUrl`) на все три действия |
| Запись пустой правки писала строку в летопись | Кнопка записи заперта, пока нечего записывать; «Renunță» заперта на время запроса |
| Enter в поле заметки перестал записывать (у старой страницы это делала форма) | Возвращено вручную |
| Неполная строка зуба в JSON молча обнуляла рецессию, кровоточивость и степени | Строка зуба — единица: неполная даёт 422, а не тихие нули |
| Дата осмотра бралась в UTC — вечерний осмотр показывался вчерашним днём везде, включая печать и §4 формы 043/e | `_day` переводит в зону клиники (только значения С поясом); держит `suite_day` |
| Переименование врача стирало подпись прошлого осмотра при следующем сохранении | `_perio_doctor` принимает имя, равное УЖЕ стоящей подписи: снимок больше нигде не хранится |
| Номер осмотра больше 2^63 в адресе давал голый 500 вместо конверта (и на форме тоже) | `_exam_id` — общая граница для формы и API; несуществующий осмотр при снятии теперь «не найден», а не «в нём есть измерения» |
| Строка зуба, из которой не вышло ни одного показания (дробные миллиметры), молча УДАЛЯЛА измеренный зуб с ответом 200 | 422: «стереть» выражается тем, что зуб не прислан, хотя назван в `covers` |
| Номера зубов строками в `covers` молча не стирали ничего, тогда как форма стирала | приняты и строки — расхождение форматов не должно менять смысл |

⭐ **Побочная находка не про продукт, а про СТЕНД:** `tests/harness.py`
поднимал сервер с трубой (`stdout=PIPE`), которую никто не вычитывал. Стоит
серверу напечатать больше буфера окна — 17 КБ трейсбека хватает, — и он
встаёт на `write` навсегда: набор выглядит как «зависло на ровном месте», а
причина не печатается, потому что застряла в той же трубе. Теперь вывод идёт
в файл рядом с песочницей (`Server.log_text()`), и падение при старте
по-прежнему видно.

⭐ **Закрыт пункт контракта, висевший с C21:** инспектор одонтограммы
показывает последний пародонтальный замер выбранного зуба (`perio.tooth_lines`
в модели одонтограммы) и ведёт в тот же осмотр. Фраза собирается на сервере —
по той же причине, что `sfx` у поверхностей. У зуба без замера блока нет.

⛔ **Находка про два рабочих места закрыта отдельным решением** — следующий
раздел. Внутри переноса её чинить было нельзя: правило меняется у ОБОИХ
интерфейсов, и выбор делал человек.

## 18.09.2026 — правило записи осмотра: сообщаем о ТРОНУТОМ

Решение Олега по последнему открытому пункту этапа 11 — «потеря зуба при двух
рабочих местах». Дыра была НЕ про React: старая форма теряла тот же зуб с
08-25, то есть ровно столько, сколько живёт сама пародонтограмма.

**Что было.** Экран присылал лист ЦЕЛИКОМ и отдельным полем `shown` — «какие
зубы я показывал», — а сервер стирал «показанные, но не присланные».
Показывает лист всегда все 32 зуба, поэтому место, открывшее осмотр раньше,
своим сохранением сносило зуб, который только что измерило второе, а чужие
правки возвращало к числам, которые видело само. Ответ 200 ok_perio, в
летописи обычная вторая строка, на экране ничего.

**Что стало.** Поле называется `covers` и значит другое: «зубы, О КОТОРЫХ это
сохранение сообщает». Стирается `covers` минус присланные — и только это. Зуб
вне `covers` не пишется и не стирается: про него не сказано ничего.

- ⭐ Суть не в имени поля, а в том, что клиент теперь считает НАМЕРЕНИЕ, а не
  показанное. Старая форма снимает слепок листа при загрузке (`wire(col)` в
  CHART_JS) и перед отправкой сравнивает с ним каждую колонку; React сравнивает
  черновик с базой (`usePerio`). Неизменённые «Medic» и «Notă» не отправляются
  вовсе: «поля нет» значит «не сообщали», иначе стала́я вкладка стирала бы
  чужую подпись осмотра тем же способом, каким стирала зуб.
- ⛔ Правило ОДНО на оба интерфейса и живёт в `db.perio_save`: у клиник сегодня
  работает старая форма (React-флаги у пилота выключены), и разойтись двум
  экранам на одних данных нельзя. Обе ветки держит `suite_two_seats`.
- ⭐ **Отпечаток осмотра** (`perio.rev` — sha256 от строк, подписи и заметки;
  колонки в базе НЕТ, миграции тоже) ездит с моделью туда и обратно.
  Разошёлся — запись всё равно проходит, но ответ иной: `ok_perio_merged`,
  «examenul a fost modificat și de pe alt calculator». Это слово, а не замок:
  409 отнимал бы у ассистента набранный лист из 192 чисел ради конфликта,
  которого чаще всего нет — места правят РАЗНЫЕ зубы.
- ⚠️ Зеркальное правило у клиента: при пересадке черновика (`rebase`) зуб,
  которого мы не трогали, берётся У СЕРВЕРА. Без этого чужой зуб, приехавший с
  ответом, показался бы пустым, лист висел бы «несохранённым», а следующая
  запись назвала бы этот зуб тронутым и стёрла его — дыра вернулась бы через
  клиента при исправленном сервере.
- ⚠️ Сообщать нечего — не делается НИЧЕГО, включая строку летописи: пустое
  сохранение это не находка осмотра, а промах по кнопке.
- **Чего решение не закрывает:** если два места правят ОДИН зуб, побеждает
  последний. Молча этого больше не происходит (`ok_perio_merged`), но слияния
  по отдельным точкам нет и не планируется.
- **Проверки.** `suite_two_seats` переписан под новое правило, а не удалён:
  два места через API и та же пара на старой форме, стирание зуба через
  `covers` без `teeth`, отсутствие ложной тревоги на свежем отпечатке, пустое
  сохранение без строки в летописи. В vitest — три: шлём только тронутое,
  очищенный зуб назван в `covers`, чужой зуб переживает следующую запись.
  ⛔ Возврат к «показывали, но не прислали» обязан краснеть.
  ⚠️ JS СТАРОЙ формы наборам недоступен — они шлют поля напрямую, — а решает
  теперь именно он. Его проверяет `scripts\perio_form_check.py` (Edge + CDP,
  не CI, механика от `odo_shots`): слепок листа, сборка `covers`, отказ
  отправлять неизменённые подпись и заметку, чистая консоль.

## 18.09.2026 — этап 12: неделя журнала (C24)

Первый экран журнала. Порядок тот же, что у C16–C23, но перед ним — два
набора, которых раньше не было: без них перенос терял бы поведение молча.

- **Контракт живого протокола** (`test_admin.suite_live_stable`, 11): два
  запроса подряд при неизменных данных обязаны дать ПОБАЙТОВО одно тело и
  один отпечаток — на всех четырёх живых экранах; новые данные отпечаток
  менять обязаны. ⭐ Соседний `suite_live_swap` сравнивал обёртку с
  фрагментом В ПРЕДЕЛАХ одного запроса — такое равенство держится само собой,
  потому что md5 считается от одной строки. А детерминированность не следует
  ни из чего: одной серверной строки с минутами хватило бы, чтобы подмена
  пошла на каждый опрос и мигание вернулось. Проверено мутацией.
  Заодно закреплено: минуты ожидания уезжают `data-wait-since` для panel.js,
  а не текстом сервера.
- **Пин недели** (`test_admin.suite_week`, 23): до 18.09 страница была
  покрыта «отвечает 200» и участием в живом протоколе — всё, ради чего она
  существует, не проверялось ни одним символом. Пин закрепил диапазон,
  навигацию ±7 дней, «сегодня» ровно в одной колонке и только на текущей
  неделе, счётчик без заметок стойки, содержимое чипа, пунктирную заметку,
  зачёркнутую неявку, исчезающую отмену, цвет переменной темы.
  ⛔ Дни сверяются СПИСКОМ ДАТ, а не количеством: колонок 5, 6 или 7, и
  раскладка по `weekday()` поставила бы субботу под воскресенье —
  правдоподобно и неверно.
- **Вынесено** в `schedule/week.py`: `monday_of`, `week_span`, `by_day`,
  `day_rows`, `day_count`, `shown_days`, `model`. Обработчик стал обёрткой,
  разметка не изменилась НИ НА БАЙТ — сверено запуском старого и нового кода
  на одной песочнице (2907 = 2907). Цвет передаётся функцией аргументом:
  `_svc_colors` общий с днём и агендой, а импорт `routes` из `week` замкнул
  бы круг.
- ⭐ Попутно: неделя ходила в базу СЕМЬ раз за отрисовку, по разу на день.
  Теперь один запрос на всю неделю и раскладка по дням в памяти — так же, как
  давно делает карточка врача (`doctors/routes._week_cells`).
- **API:** `GET /api/schedule/week[?date=]` — колонки списком, чипы с цветом
  переменными темы, итог, границы и соседние недели, день открытия для ссылок
  «Zi». Паритет со страницей держит `test_schedule_api.suite_api` (18):
  сверяются список дат, счётчики, подписи, «сегодня», содержимое чипов, цвет
  и итог.
- **React `features/schedule/`:** `week.ts`, `WeekScreen.tsx` на тех же
  классах panel.css (.week, .wcol, .wh, .wb, .wchip, .nav). Дни рисуются
  списком сервера; соседняя неделя ЗАПРАШИВАЕТСЯ, а не считается на клиенте.
  Клиент — 8 проверок, включая «скрытое воскресенье не сдвигает субботу».
- ⛔ **Главное этапа:** под флагом у недели НЕТ ни обёртки `#live`, ни метки
  опроса, а `/admin` с тем же ключом `dash` остаётся живым. Ключ снимать
  нельзя до C26 — вместе с неделей погас бы день. Держит
  `test_schedule_api.suite_switch` (10).

⚠️ Расхождения со старой страницей: переход на соседнюю неделю не
перезагружает страницу (адрес меняется `replaceState`, данные приезжают
запросом); живого опроса у недели больше нет — свежесть даёт переход, как и
было до 08-20.

⚠️ Кадры: разовая сверка старой и React-недели в headless Edge на одних
данных дала ноль расхождений (колонки, чипы, подписи, консоль, отсутствие
горизонтальной прокрутки на 1500 и 1100). Постоянного скрипта сцен у недели
нет намеренно: геометрия здесь — обычная сетка panel.css, и стеречь её
отдельно значило бы проверять CSS ради проверки.

## 18.09.2026 — этап 13: день журнала (C25)

Порядок задал Олег и потребовал раздельных коммитов: контракт построителя →
пины страниц → вынос правил → паритет → React. Каждый шаг проверялся до
следующего.

- **C25.1 — контракт `_grid`** (`tests/test_grid.py`, 36 в трёх наборах).
  Построитель рисует и «Toți medicii», и день врача, поэтому контракт стоит
  ОТДЕЛЬНО от пинов страниц и проверяет функцию: строки подаются словарями,
  часы клиники подменяются, сервер не поднимается. Закреплены непрерывный
  диапазон рядов, «pauză» против «închis», четыре исхода ячейки, мишень
  переноса, содержимое карточки, детерминированность. Измерение врача и
  легаси-имена — своими наборами.
  ⭐ Поймана ловушка В САМОМ разборщике: первая версия читала только
  СОДЕРЖИМОЕ `<td>`, а `class='goff'` и мишень `data-dk` живут в атрибутах
  тега — проверка про перетаскивание зеленела по неверной причине
  (`data-dk` встречается и внутри карточки). Зелёный CI при сломанном
  договоре переноса — ровно то, ради чего набор и писался.
- **C25.2 — пины страниц** (17): что каждая страница отдаёт построителю и
  куда кладёт результат. Колонки «Toți medicii» сверяются с `/api/doctors`, а
  не с догадкой о фикстуре; фильтр по плитке меняет СПИСОК, но сетка не
  редеет; день врача — подмножество общего дня, проверенное как подмножество.
  ⚠️ Вторая ложноположительная проверка: состояния `inactiv` у врача нет,
  движок знает `activ`/`concediu`/`arhivat`, а «inactiv» — только подпись.
  Пин переводил врача в несуществующее состояние, сохранение молча ничего не
  делало, и проверка про историю была зелёной над нетронутым врачом.
- **C25.3 — вынос правил** в `schedule/day.py`: `active_map`, `hours_range`,
  `closed_kind`, `now_hour`, `column_spec`, `work_hours`, `cell_kind`,
  `cell_rows`, `can_drop`. Разметка не изменилась НИ НА БАЙТ: сверены целые
  документы ОБЕИХ страниц на одной песочнице (36738 = 36738 и 27299 = 27299).
  Стоп-критерий Олега («если правило не выносится без правки разметки —
  разметка остаётся») не сработал ни разу.
- **C25.4 — модель и паритет** (`day.model`, `GET /api/schedule/day`, 21):
  сверяются имена и подписи колонок, метки часов и их порядок, вид закрытого
  часа, подсветка текущего, исход каждой ячейки, мишень переноса, номера
  записей по ячейкам — и поля самого перетаскивания (`min`, `dur`, `busy`,
  `movable`), потому что это договор с `panel.js`, один на оба экрана.
  Легаси-колонка — своим набором: такую строку не создать через интерфейс,
  она делается прямо в базе песочницы (так выглядят клиники с версий до
  v1.7.1).
  ⚠️ Третья моя неверная проверка: запись-сироту ТАЩИТЬ МОЖНО — она лежит в
  колонке настоящего врача, найденная по снимку имени, и адрес у переноса
  есть. Правило «переносить оттуда некуда» про колонку-сироту БЕЗ id, а такие
  строит канва дня — это C26.
- **C25.5a — React, только чтение** (`schedule_all`, `schedule_doctor`): один
  экран на обе страницы, сетка на классах panel.css. Слово статуса и возраст
  переложены на сервер — иначе в браузере завёлся бы второй словарь статусов
  (ломалось дважды, 08-12 и 08-16) и свой счёт возраста.
  ⛔ Живой опрос снят у обоих экранов, панель дня (`dash`) осталась живой —
  отдельная проверка. Ключ `prog` теперь без живых страниц, но снимается он
  не здесь: правило держится телом страницы, а не именем ключа.
  ⏳ **C25.5b** — форма записи, модалки визита и переноса, перетаскивание.
  Договор переноса живёт в `panel.js` и требует своей проверки; половина
  договора хуже его отсутствия, поэтому «+» пока ведёт в старую страницу того
  же часа.

## 19.09.2026 — этап 14: день журнала, взаимодействие (C25.5b)

Порядок тот же, что у C25.1–C25.5a, и коммиты раздельные: пины → вынос
правил → API → React. Перед первым шагом — разведка четырёх поверхностей
(форма, диалог «+», карточка, перетаскивание) с одним вопросом к каждому
правилу: «как это можно потерять в React, чтобы НИ ОДИН тест не покраснел».
Три ответа из неё и стали самым дорогим в этапе.

- **C25.5b.1 — пины** (`tests/test_day_forms.py`, 66 в четырёх наборах): что
  страницы отдают браузеру. Три места, где перенос разошёлся бы молча:
  ⛔ **Список врачей ФОРМЫ — не колонки сетки.** `/admin/all` кормит форму
  `eng.ACTIVE_DOCTORS`, а сетку — активными ПЛЮС выключенными, у которых есть
  записи дня. Возьми React список из модели сетки (первое, что просится) —
  регистратура выберет выключенного и получит `bad_off` на ровном месте. У
  разработчика оба списка совпадают, поэтому промах невидим.
  ⛔ **Комментарий в карточке ПОЛНЫЙ, в сетке обрезан до 60.** Привяжи
  текстовое поле к карточке сетки — и пересохранение без единой правки
  укоротит текст (прайор 08-16).
  ⛔ **Перенос «на своё же место» сервер ПРИНИМАЕТ** (`ok_move` и строка в
  летописи пациента). Значит охрана «бросили туда же — ничего не делать»
  обязана жить в браузере: это правило, а не украшение.
  Плюс то, чего не было ни в одном тесте: пустое окно врача (съеденное
  обедом) подменяется часами клиники — иначе пустой `<select>` уехал бы без
  поля `atime` и вместо отказа вышел бы 422; обед выпадает и из концов
  блокировки (закончить в 14:00 нельзя); `CS_SHOW` — транспонированный
  `_ACT_BUTTONS`, и это сверяется статус за статусом.
  ⭐ Каждая из 66 проверок сломана руками — 21 мутация в КОПИИ `bot\`
  (подменён `harness.BOT`), все краснеют.
- **C25.5b.2 — вынос правил**: `day.form_spec`, `day.note_ends`,
  `visits.status_actions` (одна матрица вместо `_ACT_BUTTONS` + литерала
  `CS_SHOW`) и пять правил POST — `_add_appt`, `_add_note`, `_move_appt`,
  `_set_status`, `_set_comment`; обработчики форм стали обёртками. Разметка не
  изменилась НИ НА БАЙТ: 15 целых документов (обе дневные страницы, панель,
  отфильтрованная и с предвыбором, на трёх профилях клиники) сверены до и
  после. ⚠️ Эталон пришлось переснять: первый снимок был сделан накануне, и
  смена суток сдвинула `data-hash` живого тела — сравнение врало бы.
- **C25.5b.3 — API**: модель дня получила `form`, `cards`, `actions`,
  `note_ends`; пять маршрутов `POST /api/schedule/*`. Запрос говорит, ГДЕ
  ЭКРАН (`?date=&doctor=`), тело — что делать, а удача отвечает свежим днём
  ЭТОГО экрана: запись на другую дату возвращает тот день, на который
  смотрят, ровно как `back` у старой формы. Паритет кодов — таблицей: один
  ввод проходит формой и API, коды обязаны совпасть.
  ⚠️ Таблица нашла разницу ТРАНСПОРТА, а не правил: ПУСТОЕ поле формы до
  сервера не доезжает вовсе (Starlette отбрасывает пустые значения
  urlencoded), и `Form(...)` отвечает 422 раньше нашего кода, тогда как через
  JSON пустая строка доезжает и получает честный `bad_name`. Сравнение идёт
  пробелами: их браузер пропускает, сервер обрезает, оба пути отвечают
  одинаково.
  ⛔ Одно расхождение оставлено осознанно: статус не из конвейера старая
  страница глотает молча, а API отвечает 422 — вкладка, приславшая
  несуществующий статус, обязана это услышать.
- **C25.5b.4 — React**: форма, диалог «+» с обеими вкладками, карточка визита,
  перенос. Договор перетаскивания вынесен в `move.ts` без единого узла DOM,
  и каждое из четырёх полей ломается своей проверкой. Подсказка о занятости
  считается ПО МОДЕЛИ, а не обходом `[data-busy]` в DOM, и это заодно чинит
  слепое пятно старого кода: блоки колонки-сироты не имеют `data-dk` и
  отсеивались первой же строкой обхода, хотя сервер их учитывает.

⚠️ **Две ловушки среды, обе оставляли зелёную проверку над сломанным
правилом.** jsdom возвращает всем элементам нулевой прямоугольник, а половина
часа считается ИМЕННО по нему — без подмены любой бросок оказывается на `:30`.
И `fireEvent.drop(el, {clientY})` координату роняет: `DragEvent` в jsdom нет,
обработчик получает `undefined`. Проверка «верх ячейки → ровный час» зеленела
по неверной причине, пока событие не стало синтезироваться мышиным с тем же
именем.

⭐ **Настоящее перетаскивание проверяется в настоящем браузере** —
`scripts/day_shots.py`: headless Edge через CDP, `Input.setInterceptDrags` +
`Input.dispatchDragEvent`, то есть drag начинает сам браузер. Шесть сцен: день,
диалог «+», запись, набранная руками, карточка с полным комментарием, диалог
переноса после броска в НИЖНЮЮ половину часа (11:30, а не 11:00) и переехавшая
запись. Бросок в верхнюю половину сцену краснит — она измеряет правило, а не
соглашается с ним. ⛔ Не в CI и не в `.\dev test`: нужны Edge и
websocket-client в системном Python.

⚠️ Расхождения со старой страницей, записанные осознанно:
- каждое действие отвечает свежим днём, страница не перезагружается — и
  починка прокрутки из `panel.js` не нужна: место на экране не теряется;
- диалог «+» создаётся на каждую ячейку, поэтому набранное и брошенное имя не
  подставляется на следующей (у старой страницы диалог один на документ);
- `?msg=` в адресе React-экран не рисует — как и все остальные React-экраны:
  плашка приезжает с ответом самого действия.

⛔ **Чего у React-дня на этом шаге НЕТ:** списка дня (`Lista zilei`),
плитки-фильтра `?f=` и ссылки на Excel. Вместе со списком недоступно УДАЛЕНИЕ
заметки стойки: кнопки «Șterge / Restabilește» живут только там, в карточку
визита заметки не попадают по замыслу. Закрыто следующим этапом.

## 19.09.2026 — этап 15: список дня (C25.5c)

Решение Олега: отдельным этапом и ПЕРЕД C26 — чтобы день журнала переезжал
законченным, а не таким, где часть работы внезапно остаётся в старой
странице. Размер подтвердился: новых маршрутов не понадобилось, кнопки списка
бьют в тот же `POST /api/schedule/appointments/{id}/status`.

- **Пин** (`test_day_forms.suite_list`, 21): что в строке, какие кнопки и что
  трогает плитка. ⛔ Главное записанное: ОДИН И ТОТ ЖЕ комментарий живёт на
  экране в ТРЁХ длинах — полный в `cards`, 80 знаков в списке, 60 в карточке
  сетки. Это третий способ споткнуться о прайор 08-16, и теперь все три
  длины закреплены одной проверкой. Плюс жизненный цикл заметки («Șterge», а
  после — «Restabilește» со СВОИМ вопросом), метки напоминания и дневника,
  и МЕСТО списка: при отборе он стоит НАД сеткой, без отбора — под формой.
  11 мутаций в копии дерева, все краснеют. ⚠️ Две из них сначала краснели не
  по той причине (одна меняла текст ссылки, а пин читает адрес; вторая ломала
  файл синтаксически) — мутация, которая краснеет не по адресу, не
  доказывает ничего.
- **Вынос:** `core/visits.list_rows` — строка списка данными. ⛔ Именно в
  `core`, а не в `schedule/day.py`, где начиналось: `_list` печатает и
  КАРТОЧКА ВРАЧА, а core не имеет права смотреть в модуль. Разметка не
  изменилась ни на байт на 18 документах — карточка врача добавлена в сверку
  ровно поэтому.
- **Модель:** `list`, `note_actions`, `filter` и сам отбор. Фильтр режет
  ТОЛЬКО список: сетка отвечает на другой вопрос — что стоит в дне, — и
  прореживать её значило бы соврать про занятость. ⚠️ Отбор едет с КАЖДЫМ
  запросом экрана: ответ действия без `f` подменил бы отфильтрованный список
  полным, и запись «исчезла бы» из фильтра на глазах. У дня врача отбора нет
  вовсе, как и у старой страницы, а неизвестный `f` не доезжает даже до узла
  React.
- **React:** `DayList` на тех же классах (`.list`, `.act`, `.stat`,
  `.rem-mark`, `.rec-mark`), кнопки — с сервера, комментарий строки не
  правится (полный живёт в карточке).

⭐ Дыра, ради которой этап и делался, закрыта и ПОКАЗАНА в настоящем браузере:
сцена 07 стенда убирает заметку из списка, и её блок исчезает из сетки. Сама
сцена сперва покраснела и нашла ловушку стенда: CDP шлёт мышь в координатах
ОКНА, а список лежит ниже сгиба — клик уходил в пустоту, и выглядело это как
«кнопка не работает». Теперь элемент подводится к середине экрана
(`click_below`), и это записано там же: та же ошибка ждёт любую сцену, которая
тронет что-нибудь под сеткой.

⚠️ Расхождение, оставшееся осознанно: список карточки ВРАЧА (`doctor_card`,
C14) по-прежнему без кнопок статуса — он печатает тот же `_list`, но React-
экран врача рисует свою укороченную таблицу. Теперь для него есть готовая
модель (`list_rows`), и закрыть это можно одной правкой — отдельно от C26.

## 19.09.2026 — этап 16: панель дня — контракт, пины, вынос (C26.1–C26.3)

Порядок прежний: контракт → пины → вынос правил → паритет → React. Здесь
первые три шага; модель с паритетом (C26.4) и React (C26.5) — следующие.

- **C26.1 — контракт панели** ([admin-contract.md](admin-contract.md)).
  Разведка отвечала на один вопрос: годится ли для канвы модель дня
  (`schedule/day.py:model`), которая уже возит React-таблицу. ⛔ Не годится, и
  главное расхождение — не геометрия, а КОЛОНКА. Легаси-строка без
  `doctor_id`, но с именем ЖИВОГО врача, на канве получает СВОЮ колонку рядом
  с его собственной (подпись «în afara listei · N prog.» и форма relink), а
  `cell_rows` таблицы ищет по ОБОИМ ключам и такую строку СЛИВАЕТ. Канва —
  единственное место в программе, где видны записи выпавшего из справочника
  врача, и единственный вход в `POST /admin/relink`; React, собранный по
  модели таблицы, не нарисовал бы их вовсе: визит исчезает с панели, час
  выглядит свободным. ⚠️ И `test_schedule_api.suite_day_orphan` при этом
  осталась бы ЗЕЛЁНОЙ — она про таблицу и фиксирует как раз слияние. Плюс
  геометрия (`base_min` первого ОСТАВЛЕННОГО часа, кластеры) и состав шапки,
  которых в модели дня нет вовсе.
  ⭐ Побочный улов: **60 проверок пережили бы переезд зелёными** — фикстуры
  поднимают сервер без флага `ui.react`, и они продолжили бы читать СТАРУЮ
  страницу. Четыре формы ложного зелёного выписаны там же наизусть (пустая
  последовательность, отрицание, чтение файла вместо поведения, порог с
  запасом); одна — `all([])` — найдена и починена сразу.
- **C26.2 — пины** (`tests/test_admin_canvas.py`, 7 наборов, 69 проверок).
  Порядок задан ЦЕНОЙ ПОЛОМКИ, а не удобством: сирота и relink (16) →
  геометрия и кластеры (8) → «число плитки = число по её фильтру» (4) →
  шапка врача (10) → мини-календарь (9) → повестка (14) → тренды и их
  ОБРАТНАЯ полярность (8). ⛔ Кластер пинуется ЦЕПОЧКОЙ A(09:00–11:00),
  B(09:30–10:00), C(10:30–11:00): B и C между собой не пересекаются, но обе —
  с A, и сравнение «с последней в кластере» откололо бы C и поставило её во
  всю ширину ПОВЕРХ A — спрятанный пациент, выглядящий правдоподобно.
- **C26.3 — вынос правил** в `schedule/canvas.py` (11 функций: `row_col`,
  `bounds`, `pos`, `clusters`, `row_hours`, `hours_of`, `open_hours`,
  `trim_edges`, `free_hour`, `shown_doctors`, `orphan_cols`). Разметка не
  изменилась ни на байт: сверены 18 ЦЕЛЫХ документов — шесть страниц на трёх
  профилях клиники. Стоп-критерий Олега не сработал ни разу.
  ⚠️ Место, где вынос мог соврать молча, ровно одно: `_pos` читал `base_min`
  ЗАМЫКАНИЕМ, а тот пересчитывается ПОСЛЕ среза краёв — у вынесенной `pos`
  он стал аргументом. Сверка эту ветку ИСПОЛНЯЕТ: у `clinic_hours` срезаны
  оба края, у `clinic_split` — верхний, и `base_min` после среза у обоих
  другой; колонка-сирота и кластер пересечения есть во всех трёх профилях.

- **C26.4 — модель и паритет** (`canvas.model`, `GET /api/schedule/canvas`,
  три набора, 32 проверки). Модель у канвы СВОЯ, и маршрут тоже свой: взять
  клиенту панели `/api/schedule/day` — это ровно та ошибка, ради которой
  писалась разведка. Вид самой записи при этом общий — `day.appt_view`, те же
  поля переноса (`min`, `dur`, `busy`, `movable`), что печатает `_move_attrs`;
  канва дописывает к ним только геометрию (`top`, `height`, `col`, `of`).
  ⛔ Порядок внутри кластера отдаёт СЕРВЕР, готовым `col`: пересортируй React
  сам — и ему понадобится свой список «живых» статусов, тот самый второй
  словарь, который ломался дважды.
  ⚠️ Чего в модели нет намеренно: линия «сейчас» (двигается непрерывно —
  `panel.js`), ступени сжатия блока (ставятся ПО ЗАМЕРУ: высоту ряда решает
  окно, порог числом соврал бы на одном из двух мониторов) и пиксельные
  врезки. Подсветка текущего ЧАСА, наоборот, серверная — она меняется раз в
  час, и это честное изменение страницы.
  ⭐ Два числа в ОДНОЙ строке шапки питаются РАЗНЫМИ списками, и это не
  описка: «N prog.» считает пациентов (заметка стойки не в счёт), а
  «liber HH:00» смотрит на занятость (заметка занимает час наравне с визитом).
  Сведи их к одному списку — модель начнёт звать записываться в час, который
  заблокирован, и сервер откажет. Держит отдельная проверка.
  ⚠️ Процент загрузки НЕ обрезан сотней: обрезается только ШИРИНА полосы.
  Перебронированный день показывает «130%», и это единственный признак, по
  которому директор его увидит.
  ⛔ Список relink — ВЕСЬ справочник, включая выключенных врачей: собери его
  из показанных колонок, и записи выпавшего врача станет некуда переприкрепить
  в тот день, когда нужный врач в отпуске.
  ⛔ Цвет колонки канва считала по месту среди ПОКАЗАННЫХ врачей, а весь
  остальной продукт (`core.visits._doc_hue`) — по месту в справочнике. В C26.4
  модель повторила страницу НАМЕРЕННО: паритет там был важнее правоты, иначе
  покраснел бы `suite_model`. Сведено к одной формуле следующим коммитом —
  отдельным, потому что это правка ПОВЕДЕНИЯ; разбор ниже.
  Убрана мёртвая арифметика, оставшаяся от выноса: `idx` присваивался дважды
  и не читался ни разу, `_r_bounds` — тоже. Разметка от этого не изменилась.

- **Цвет врача — одна формула** (19.09, ОТДЕЛЬНО от C26). Обе половины —
  страница (`schedule/routes.py`) и модель канвы (`schedule/canvas.py`) —
  переведены на `core.visits._doc_hue` ОДНИМ коммитом: порознь краснеет
  паритетный `suite_model`, и читается это как поломка модели, а не как
  половина правки.
  ⛔ Правка МЕНЯЕТ разметку `/admin`: у врача без своего цвета колонка стала
  другого цвета. Внутри шага C26 ей места не было — там стоп-критерий
  «разметка не меняется ни на байт», и разошедшийся байт-снимок означал бы
  регрессию. Здесь он ожидаем, и коммит говорит об этом прямым текстом.
  ⚠️ Дефект был двойной, и вторая половина хуже первой. РАСХОЖДЕНИЕ: у врача
  без своего цвета колонка одного цвета, его же аватар на карточке — другого.
  ПЛАВАНИЕ: список показанных колонок зависит от того, у кого есть записи В
  ЭТОТ ДЕНЬ, поэтому цвет колонки менялся ото дня ко дню. Регистратура держит
  цвет за признак человека, а не дня.
  ⭐ Правоту цвета не проверял НИ ОДИН набор, и это не недосмотр, а форма
  ложного зелёного: `suite_model` сверяет модель со СТРАНИЦЕЙ, а врали обе
  половины одинаково. Пин (`suite_hue`) поэтому сверяет цвет колонки с ДРУГИМ
  экраном — с аватаром на карточке врача и с `/api/doctors`, — и отдельно
  два дня подряд, в которых показаны разные наборы колонок. До правки он
  краснел дважды.
  ⭐ Чтобы третья формула не завелась, `test_structure.py` держит палитру
  `_DOC_HUES` внутри `core/visits.py`. ⚠️ Полярность у правила опасная — оно
  ищет ИМЯ, поэтому при нём якорь: `_DOC_HUES` и `_doc_hue` обязаны быть
  ОПРЕДЕЛЕНЫ в модуле-владельце, иначе переименование выключило бы сторожа
  навсегда, и ноль нарушителей читался бы как порядок.

⭐ **Три ловушки самих ПРОВЕРОК, пойманные в C26.4** — все три выглядели бы
поломкой модели, а были поломкой набора:

1. **Второе имя `_DCARD` в том же файле.** Разбор шапки для паритета назвали
   так же, как разбор шапки для `suite_head`; питон молча оставил последний, и
   соседний набор упал на `int('liber 12:00')` — с номерами групп чужого
   регулярного выражения. Одно имя — одна вещь, и в тестах тоже.
2. **Фикстура, зависящая от порядка строк.** `_ids` читает день по ВРЕМЕНИ, а
   заметка, заведённая до разбора id, встала первой и сдвинула разбор на
   единицу: 120 минут уехали на заметку, кластер не собрался. Поймал якорь —
   и назвал ровно то, чего не хватает.
3. **Якорь по СЛОВАМ, а не по месту.** «Zi liberă» печатает не только карточка
   вместо сетки, но и баннер над журналом, поэтому якорь пустого дня зеленел
   бы на дне, где сетка есть. Теперь ищется карточка.

⭐ Заведён профиль `clinic_panel.json`: закрытое воскресенье и заполненные
кабинет с телефоном. Обе ветки не исполнялись НИ ОДНИМ прежним профилем —
`if meta.get("room")` пуст на демо-данных (ровно тот случай, когда у
разработчика всё открывается, а у клиники с заполненным полем нет), а «Zi
liberă» не показывал ни один: все три профиля открыты семь дней в неделю.

⭐ **Грабля самой СВЕРКИ**, найденная при независимой перепроверке (снимок с
родительского коммита в отдельном worktree против рабочего дерева): адрес
статики несёт `?v=` из **mtime файла**, а свежий checkout ставит mtime
«сейчас». Расходятся ВСЕ восемнадцать документов — в месте, к правке
отношения не имеющем. Сверять такие снимки только с нормализацией `?v=\d+`,
иначе зелёный вынос выглядит красным, а следующим шагом идёт «починка»
страницы под несуществующее расхождение.

⏳ Осталось от плана пинов: восьмой — «живой протокол как ДАННЫЕ, а не
разметка». Это C27, но пин нужен раньше — до того, как живая сетка переедет
в React.

⚠️ **Чего C26.5 не должен забыть** (найдено при разборе разметки под модель):

- **`panel.js` строит карту «ключ врача → имя» РАЗБОРОМ ссылок** `/admin/doctor/{dk}`
  в шапке и пользуется ею в диалоге переноса («Dr. X · 10:00 → Dr. Y · 11:00»).
  React-канва без таких ссылок оставит в диалоге «—» с обеих сторон, и
  подтверждать перенос придётся вслепую. Имена в модели уже есть
  (`columns[].id` / `name`) — либо брать оттуда, либо сохранить ссылку.
- **Мишень броска — ДВА признака сразу**: `.gcell[data-h]` у часа и
  `.gcol[data-dk]` у колонки. У сироты нет ни того, ни другого, и это одно
  правило в двух местах: включат одну половину — вторая перестанет держать, а
  проверка на первую останется зелёной.
- **`?msg=no_access` приземляется именно на `/admin`** со ВСЕЙ программы
  (двенадцать проверок прав сверяют редирект сюда). React-панель обязана
  показать `?msg=` из адреса при ПЕРВОЙ отрисовке, а не только после своих
  ответов — иначе отказ в правах станет молчаливым переходом на панель.
- **Знак `′` (U+2032)** в теле блока и в подсказке — он в вшитом подмножестве
  Inter и проверку «интерфейс не просит знаки у Windows» проходит. React несёт
  ТОТ ЖЕ символ, а не апостроф.

## 19.09.2026 — этап 17: панель дня — протокол и каноническое значение (C26.5.2, ступени 1–2)

⛔ Порядок задан Олегом: **сначала закрыть развилку протокола, и только потом
писать экран**. Причина названа им же — «можно построить React-панель поверх
протокола, у которого нет симметричного отката». Экрана в этом этапе нет ни
строчки.

- **C26.5.2-a — базовая линия, без единой правки кода**
  ([live-contract.md](live-contract.md) › 6c). Матрица снята на живом сервере
  в двух профилях. Три находки меняли план, и все три — опытом, а не чтением.
  ⛔ При включённом флаге канал отвечал `live:false` и ПУСТЫМ составом: React-
  панели брать состояние было бы негде. При этом ветку `live:false` не
  потреблял ни один живой клиент — старая вкладка опрашивает АДРЕС СТРАНИЦЫ и
  получает 205 от `_live_stale`.
  ⛔ Обратной половины отката не было вовсе: при ВЫКЛЮЧЕННОМ флаге канал
  отвечал `live:true`, то есть открытая React-вкладка услышала бы «я активна»
  ровно тогда, когда сервер уже отдаёт по `/admin` старую страницу.
  ⭐ **Дыра детерминизма оказалась не там, где её назвал §4.1.** Тот раздел
  писался по разметке и для неё верен. В ДАННЫХ у заметки полный `text` уже
  лежал (его кладёт `day.appt_view` до того, как `canvas.blocks` допишет
  `title[:80]`/`label[:40]`), и правка после 80-го знака отпечаток МЕНЯЛА.
  Проекцией внутри канала сравнения оказался другой текст — `comment[:60]` у
  визита: правка после 60-го давала байт в байт прежние данные и `204`.
  ⚠️ Опыт едва не «доказал» дыру, которой нет: идентификаторы он брал разбором
  страницы, регулярка спарила имя с чужим `<td>`, и комментарий уехал на
  заметку. Признак был виден только контрольной правкой ДО границы — она тоже
  не двигала отпечаток, то есть «сломано вообще всё». Отсюда правило опыта:
  **у каждой проверки дыры обязан быть контроль, который ОБЯЗАН сработать.**
- **Ступень 1 — канал перестал спрашивать, кто рисует экран**
  ([live-contract.md](live-contract.md) › 6d). `live` стало ИМЕНЕМ ТОГО ЖЕ
  ФАКТА, что и состав конверта (`core/api.live_envelope` — единственная его
  сборка). Маршрут не зовёт ни `react_on` (читает `?ui=` САМОГО ЗАПРОСА:
  ответ, а через данные и отпечаток, зависели бы от адреса опроса), ни
  `LIVE_RELOAD` (список ЛЕГАСИ-раскладок; на C27 `dash` уйдёт из него вместе
  со старой страницей, и связанный канал молча перестал бы кормить React).
  ⭐ Поверхность уехала в заголовок `X-DP-Surface: react|legacy`, считает её
  новый `layout.react_flag(screen)` — БЕЗ запроса. Причина та же, что у
  `X-DP-V`: всё, что попадает в `data`, попадает в отпечаток. И следствие,
  ради которого это и сделано: **заголовок приезжает и на `204`**, поэтому
  откат виден в полностью тихий день, когда тела нет вовсе.
  Откат стал симметричным: 205 старой вкладке на включение флага (C26.5.1) и
  `X-DP-Surface: legacy` React-вкладке на выключение. Хвост базовой линии
  закрылся сам — `?ui=legacy` на КАНАЛЕ больше не меняет ни тела, ни
  отпечатка, ни поверхности.
  ⭐ Снят мёртвый `LIVE_HEADER` (объявлен, не читался ничем) — константа с
  включающей полярностью, пережившая бы переименование заголовка молча.
- **Ступень 2 — каноническое значение и проекция показа**
  ([live-contract.md](live-contract.md) › 6e). ⛔ Ни одна обрезка не снята и
  считает их по-прежнему сервер: правилось ИМЯ. `comment` стал полным, рядом
  встал `comment_cut` (60 в блоке сетки, 80 в строке списка), чип недели —
  `text_cut`. Канва не тронута: `title`/`label` — имена МЕСТА показа, а не
  имена значения, и полный `text` рядом уже лежал; она и была образцом.
  Проверок три, и они отвечают на РАЗНЫЕ вопросы: правка ВНУТРИ видимой
  границы, правка ЗА ней при совпадающем начале, и «полное значение доехало ДО
  КЛИЕНТА, а не только до хеша». Третья не лишняя — без неё достижимо
  «отпечаток верный, тело неверное», и нашлось бы это уже на React-диалоге.
- **Сторожа.** Два новых правила `test_structure` и пять мутаций: «маршрут
  `/api/` не спрашивает, кто рисует экран» (архитектурная граница перед C27) и
  «обрезок не носит имени полного значения». У второго ДВА якоря — по одному
  на каждую синтаксическую форму (ключ словаря и присваивание
  `v["title"] = …[:80]`): правило ищет ФОРМУ, и замены среза вызовом хватило
  бы, чтобы оно позеленело навсегда.
- **Починены две проверки, зелёные по неверной причине.** `test_admin_canvas`
  сверял длины заметки как `min(80, len(текст))` при тексте в 38 знаков — обе
  величины равнялись 38, и перестановка 80 и 40 местами прошла бы
  незамеченной. `DayScreen.test.tsx` утверждал «правится полный, а не
  обрезанный в сетке» на записи, у которой комментарий в сетке был ПУСТ.

- **Ступень 3 — React-поверхность** (`DashScreen` + `DashCanvas` + `DashRail`
  + транспорт живого канала). Экран включается ЦЕЛИКОМ, четырьмя блоками
  сразу (слово Олега 19.09): половина панели на новых данных и половина на
  старых — это два разных дня рядом.
  ⛔ **Экран ЧИТАЮЩИЙ.** Диалогов, записи по клику в пустой час и
  перетаскивания нет — это C26.5.3. Поэтому подсказка под сеткой говорит
  правду («deocamdată doar pentru citit») и ведёт в старую панель, вместо
  того чтобы обещать действия, которых экран не умеет. ⚠️ Включать флаг у
  клиники до C26.5.3 нельзя: регистратура потеряет запись по клику.
  ⭐ Транспорт живого канала — СВОЙ, не `api.request`: у канала четыре исхода
  и два из них без тела, а `204` уходил бы у общего входа в `default:` и
  приезжал как «ошибка сервера» на самом частом ответе живого журнала. Плюс
  канал обязан слать и читать свои заголовки. Правило «что делать с ответом»
  — чистая функция (`services/live.nextLive`), и `hold` в ней стоит ВЫШЕ
  перезагрузок: человек держит блок мышью, а отложить безопасно ровно потому,
  что отказ применить НЕ ДВИГАЕТ отпечаток.
  ⭐ Пиксель часа остаётся в CSS (`--cell`), код считает только МНОЖИТЕЛИ —
  то же разделение, что у блоков. Верни высоту в состояние React, и каждый
  `resize` стоил бы рендера, а замер зациклился бы сам на себе (08-17).
- **Ступень 4 — сцена в браузере двумя клиентами** (`scripts/dash_shots.py`,
  headless Edge через CDP). Она отвечает на вопрос, который не берут ни
  Vitest, ни прогон: ЦЕПОЧКА целиком — канал → опрос → состояние React →
  отрисовка → раскладка. Красивая панель, переставшая слушать протокол,
  проходит и то, и другое: первая отрисовка у неё верна.
  Главная сцена — второе рабочее место: обычный HTTP-клиент добавляет запись,
  и в пределах одного опроса блок приезжает на экран, **а геометрия соседей не
  меняется**.
  ⭐ Сцена тишины нашла ДВЕ вещи, которых не нашло ничто другое.
  ⛔ Первая: `panel.js` остаётся на React-странице (он в каркасе), и его
  тридцатисекундный `placeNowline` находит `.gridbody[data-day]`, который
  теперь рисует React, — он доклеивал бы ВТОРУЮ линию внутрь чужого
  поддерева. Охрана `DP_REACT` поставлена на три таймерные функции; кусалась
  одна, остальные две безвредны по случайности, а случайность — не основание.
  ⛔ Вторая: перемер сжатия блоков был привязан к тику ожидания вообще, и в
  дне БЕЗ ожидающих пересчитывал классы вхолостую — четыре мутации DOM в
  минуту на неизменном дне. Ровно та болезнь, от которой ушли 08-20, только
  меньшего масштаба. Теперь тик передаётся, лишь когда ожидающие есть.
  ⚠️ Окно тишины длиннее МИНУТЫ намеренно: линия пересчитывается раз в 30 с,
  но двигается только со сменой минуты, и на окне в 39 с проверка «линия жива»
  краснела бы через раз. И оба условия проверяются ВМЕСТЕ: «ноль мутаций» в
  одиночку зелено и у намертво замершего экрана.
  ⚠️ Сцена не в CI и не в `.\dev test`: нужны Edge и `websocket-client` в
  СИСТЕМНОМ Python (в `.venv-desktop` ничего не ставить — это окружение
  сборки).
- **Побочно.** `screen_map.py` вёл список флагов руками и отстал на пять имён
  — карта экранов показывала «—» у уже переехавших perio, недели, дня и дня
  врача; дописаны все, включая панель. И `smoke_exe` впервые спрашивает живой
  канал у СОБРАННОЙ программы: страницы его не задевают, `/health` тем более,
  а зависит от него, будет ли панель у клиники обновляться сама.


## Базовая линия

```powershell
.\.venv-desktop\Scripts\python.exe tests\run_tests.py
```

На 16.09.2026, версия 1.26.0: **1810/1810 за 340.5 с**, код выхода 0.

⭐ Правило проекта: вычислимое не записывать — записывать команду. Число
проверок растёт, и сессия, ждущая старого числа, однажды соврёт.

## Точка невозврата

До **фазы 7** продукт остаётся прежним exe: React живёт внутри него, старый
интерфейс на месте, обновления не тронуты, откат — обычный `git revert`.
Работу можно бросить на любом экране и остаться с работающим продуктом.

⛔ Необратимость начинается там, где `.old.exe` у клиники впервые окажется
Python-сборкой, а рабочий exe — Tauri. До этого шага копия папки установки
пилота обязана лежать офлайн.

## Что закрыто и что открыто

**Решено:** порядок работ (граница API раньше оболочки), одна папка вместо
второго репозитория, параллельная разработка с флагами, свой обновлятор вместо
Tauri-обновлятора, Inno вместо NSIS, Windows без Linux и macOS.

**Открыто и решается не кодом:**

- ⏳ **PG-ветка** — в каждой из 109 функций `db.py` два текста SQL. Если
  облако выкидывается, половина слоя данных упрощается; если SaaS в планах —
  ветку беречь. Ни один тест её не исполняет.
- ⏳ **IDNO** — держит одновременно подпись кода, запуск сайта и SMS-пакет.
- ⏳ **Копия базы пилота** для замеров (18.09 Олег: пилот переустанавливается заново — не приоритет): все выводы о скорости (список
  пациентов, статистика за 366 дней, обе дуги одонтограммы) сделаны чтением
  кода, без единого числа с живых данных.

## Побочные находки — чинятся независимо от миграции

⚠️ **6 маршрутов (18.09, пересчитано генератором) не сторожит ни один набор тестов.** Полный список —
[screen-test-map.md](screen-test-map.md). Самый дорогой: `POST /admin/update/run`
подменяет exe у клиники и не покрыт ничем. Это дыры сегодняшнего продукта.

⚠️ **У `DentPilot.exe` пустые метаданные файла.** Лечится флагом
`--version-file`, сертификата не требует — [installer.md](installer.md).

## Абсолютные правила

1. Движок не переписывать. `db.py`, `engine.py`, `core/auth.py` не трогать ради
   нового интерфейса.
2. Старый HTML в JSX не копировать — компоненты проектировать заново.
3. API не делать отрисовщиком HTML.
4. Origin не менять без отдельного решения.
5. Печать и аварийные экраны в React не переносить.
6. Обновлятор Tauri не включать. Inno автоматически не заменять.
7. `--onefile` не использовать. Сайдкар под LocalSystem не запускать.
8. Старый экран не удалять, пока новый не прошёл пилота и окно отката.
9. Тесты ради миграции не удалять и осмысленное покрытие не уменьшать.
10. Бизнес-правила ради удобства клиента не менять.
11. На боевой базе клиники экспериментов не ставить.
12. Любое изменение поведения — записывать.
