# Миграция — состояние и порядок

Точка отсчёта и полный разбор — [../migration-audit.md](../migration-audit.md).
Что программа умеет **сегодня** — [../current-functionality.md](../current-functionality.md).
Поэтапный план — [../migration-plan.md](../migration-plan.md).
Пошаговый список работ с числами — **[tasks.md](tasks.md)**.

## Доска фаз

| | Фаза | Состояние |
|---|---|---|
| 0 | аудит, базовая линия, копия данных | ✅ аудит и линия · ⛔ копия папки пилота **не снята** |
| 1 | CI, только прогон движка | ✅ файл есть · ⚠️ на GitHub ещё **не исполнялся** |
| 2 | JSON API и флаг экрана | ⏳ следующая |
| 3 | каркас React | ✅ собирается, движок его отдаёт |
| 4 | первый экран — `Setări clinică` | ⏳ |
| 5 | остальные экраны за флагами | ⏳ |
| 6 | сторожа, мутация, тесты клиента, E2E | ⏳ |
| 7 | оболочка Tauri и sidecar | ⏳ Rust не установлен |
| 8 | упаковка Inno | ✅ работает, менять не требуется |
| 9 | существующий обновлятор | ⏳ эксперимент с подменой не проведён |
| 10 | pre-release, канарейка, ручной publish | ⏳ |

⚠️ Фаза 3 сделана **раньше** фазы 2, и это осознанно: каркас нужен был, чтобы
проверить ограничение имён файлов сборки на живом сервере, а не на бумаге.

## Базовая линия

```powershell
.\.venv-desktop\Scripts\python.exe tests\run_tests.py
```

На 16.09.2026, версия 1.26.0: **1810/1810 за 340.5 с**, код выхода 0.

⭐ Правило проекта: вычислимое не записывать — записывать команду. Число
проверок растёт, и сессия, ждущая старого числа, однажды соврёт.

## Точка невозврата

До **фазы 7** продукт остаётся прежним exe: React живёт внутри него, старый
интерфейс на месте, обновления не тронуты, откат — обычный `git revert`.
Работу можно бросить на любом экране и остаться с работающим продуктом.

⛔ Необратимость начинается там, где `.old.exe` у клиники впервые окажется
Python-сборкой, а рабочий exe — Tauri. До этого шага копия папки установки
пилота обязана лежать офлайн.

## Что закрыто и что открыто

**Решено:** порядок работ (граница API раньше оболочки), одна папка вместо
второго репозитория, параллельная разработка с флагами, свой обновлятор вместо
Tauri-обновлятора, Inno вместо NSIS, Windows без Linux и macOS.

**Открыто и решается не кодом:**

- ⏳ **PG-ветка** — в каждой из 109 функций `db.py` два текста SQL. Если
  облако выкидывается, половина слоя данных упрощается; если SaaS в планах —
  ветку беречь. Ни один тест её не исполняет.
- ⏳ **IDNO** — держит одновременно подпись кода, запуск сайта и SMS-пакет.
- ⏳ **Копия базы пилота** для замеров: все выводы о скорости (список
  пациентов, статистика за 366 дней, обе дуги одонтограммы) сделаны чтением
  кода, без единого числа с живых данных.

## Побочные находки — чинятся независимо от миграции

⚠️ **13 маршрутов не сторожит ни один набор тестов.** Полный список —
[screen-test-map.md](screen-test-map.md). Самый дорогой: `POST /admin/update/run`
подменяет exe у клиники и не покрыт ничем. Это дыры сегодняшнего продукта.

⚠️ **У `DentPilot.exe` пустые метаданные файла.** Лечится флагом
`--version-file`, сертификата не требует — [installer.md](installer.md).

## Абсолютные правила

1. Движок не переписывать. `db.py`, `engine.py`, `core/auth.py` не трогать ради
   нового интерфейса.
2. Старый HTML в JSX не копировать — компоненты проектировать заново.
3. API не делать отрисовщиком HTML.
4. Origin не менять без отдельного решения.
5. Печать и аварийные экраны в React не переносить.
6. Обновлятор Tauri не включать. Inno автоматически не заменять.
7. `--onefile` не использовать. Сайдкар под LocalSystem не запускать.
8. Старый экран не удалять, пока новый не прошёл пилота и окно отката.
9. Тесты ради миграции не удалять и осмысленное покрытие не уменьшать.
10. Бизнес-правила ради удобства клиента не менять.
11. На боевой базе клиники экспериментов не ставить.
12. Любое изменение поведения — записывать.
