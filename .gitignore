# secrets & local
.env
*.log
backups/

# sales playbook & personal contacts — intentionally NOT public
pitch/
*.pdf

# python
__pycache__/
*.pyc

# desktop build & runtime
.venv-desktop/
dist/
build/
data/
dental.env
*.bak

# frontend (DentPilot 2.0)
frontend/node_modules/
# ⚠️ Результат сборки фронта кладётся ПРЯМО в статику движка, чтобы его
# подхватил существующий --add-data. В git он не едет — это артефакт.
# ⛔ Когда поедет первый React-экран, Build-Desktop.ps1 обязан звать
# `npm run build` ДО PyInstaller: иначе сборка с чистого клона положит в exe
# страницу без бандла, и увидит это только клиника.
bot/app/static/js/bundle.js
bot/app/static/css/bundle.css
bot/app/static/js/bundle.js.map
# rust (PHASE 7)
src-tauri/target/

# cloud (сервер лицензий): база, письма сухого прогона, своё окружение
cloud/cloud.db*
cloud/outbox/
cloud/.venv/
cloud/backups/
cloud/deploy/cloud.env
